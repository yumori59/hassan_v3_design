# レビュー: backend 雛形のディレクトリ骨格と規約追記 (コミット 0448a12)

レビュー対象: **コミット `0448a12` (`git diff 6bdf7ef..0448a12`)**。
先行レビュー [review-conversation-r3.md](review-conversation-r3.md):50 が「依頼により対象外」とした差分であり、
本レビューが唯一の関門。**判定は本番基準** (`.claude/rules/08-production-gates.md`。「PoC では対象外」を省略理由と認めない)。

## レビュー結果サマリ

- 対象 (リポジトリ相対パス):
  - `templates/app-monorepo/backend/STRUCTURE.md` (新規 202 行)
  - `templates/app-monorepo/backend/prompts/agents.yaml` (新規 36 行)
  - `templates/app-monorepo/backend/CLAUDE.md.tmpl` (追記: DTO / バリデーション責務分離 / 集約ルート / env)
  - `templates/app-monorepo/backend/layering-scopes.yml` (注記追加)
  - `templates/app-monorepo/frontend/CLAUDE.md.tmpl` (`env/<env>.env` 反映)
  - `templates/infra-repo/.claude/agents/infra-reviewer.md` (同上)
  - 上記に加え `templates/app-monorepo/backend/` 配下の `.gitkeep` **41 件** (空ディレクトリの骨格)
- 参照した設計 SSOT は**ワーキングツリーの現在の内容** (未コミット差分を含む)
- **重大 4 件 / 中 5 件 / 軽微 7 件**
- 実行した検証: `make check` (エラー 0 / 警告 40 = すべて既存) / 一次ソースの抜き取り照合 **12 件** (うち 1 件で**事実誤り**を検出)

### 実行した検証 (出力)

```
$ make check
[doc-lint] 対象 106 ファイル / エラー 0 件 / 警告 40 件
[traceability] construction-workflow: 24/24 カバー — OK
[traceability] productionization: 86/86 カバー — OK
[traceability] 照合 2 feature / 未カバーあり 0 feature
[workflow-shell] 検査 52 ブロック / エラー 0 件
[table-counts] 実測: 機能テーブル 42 (個人 34 / 契約 8) / 分類 ①31 ②2 ③1
[table-counts] 実測: 機能テーブル以外 12 (所有者列なし 7 / 所有者列あり 5) / 検査①の除外リスト 9
[table-counts] 照合 37 件 / エラー 0 件
[endpoint-mapping] 実測: auth-accounts.md 37 本 / 9 ドメイン 112 本 / settings.md §5 18 行 / custom tool 8 本 / 403 16 本
[endpoint-mapping] 照合 36 件 / エラー 0 件
```

警告 40 件はすべて既存 (ルール文書・過去 review・`design_memo.md` の「TODO」語と、`data-model.md` /
`frontend.md` / `infrastructure.md` ×2 / `llm-migration.md` / `operations.md` の未回答 `[Answer]` 6 件)。
`doc-lint` は `templates/` 配下の `*.md` も走査対象に含む (`scripts/doc-lint.sh:30` の `find . -name '*.md'`)
ため、`STRUCTURE.md` の相対リンク `../README.md` へのリンク は実在確認済み。
**`CLAUDE.md.tmpl` は `.md` でないため doc-lint の対象外** (軽微 7 参照)。
本ファイル保存後の再実行も **エラー 0 件** (`[doc-lint] 対象 107 ファイル / エラー 0 件`)。
警告の増分は本ファイル自身が「TODO」の語を含むことへの反応のみで、対象成果物由来のものは無い。

### 抜き取り照合の一覧 (12 件)

| # | 主張 (出典) | 照合コマンド | 結果 |
|---|---|---|---|
| 1 | `usecase/theme/create_theme.go:18` の `CreateThemeInput` (STRUCTURE.md:99) | `sed -n '14,24p' usecase/theme/create_theme.go` | **一致** (:18 が `type CreateThemeInput struct {`) |
| 2 | `controller/theme.go:137`〜 で DTO を bind → Input に変換 (STRUCTURE.md:103) | `sed -n '130,152p' controller/theme.go` | **一致** (:137 = `c.BindJSON(&req)`、:144〜 で `themeusecase.CreateThemeInput{...}` に変換) |
| 3 | `controller/dto/account.go:25` の `binding:"required,min=8,max=255"` (STRUCTURE.md:113) | `sed -n '20,30p' controller/dto/account.go` | **一致** (:25 = `Password ... binding:"required,min=8,max=255"`) |
| 4 | `entity/account.go:44` の `NewAccount` は `AuthRoleID` の enum だけを検証 (STRUCTURE.md:115) | `sed -n '38,56p' entity/account.go` | **一致** (:44 が `func NewAccount`、検証は `NewAuthRole(args.AuthRoleID)` のみ。パスワード長の検証なし) |
| 5 | **`usecase/` から `controller/dto` を import しているファイルは v2 で 0 件** (STRUCTURE.md:104) | `grep -rl "controller/dto" usecase/ \| wc -l` → **29** / `grep -rl ... \| grep -v _test.go \| wc -l` → **27** | **誤り (重大 1)** |
| 6 | `IdeaBoard.CanEdit(accountID)` が entity の純粋メソッドとして実在 (CLAUDE.md.tmpl:136) | `sed -n '108,122p' entity/idea_board.go` | **一致** (:113 `HasAccess` / :118 `CanEdit`。architecture.md:233 の `:113`〜`:118` も正確) |
| 7 | `di/` は `provider.go` / `wire.go` / `wire_gen.go` の 3 ファイル、`wire_gen.go` は生成物 (STRUCTURE.md:148) | `ls di/` / `head -2 di/wire_gen.go` | **一致** (`// Code generated by Wire. DO NOT EDIT.`) |
| 8 | **`common/` 移動で `.golangci.yml` の変更は不要** (STRUCTURE.md:67-68) | `.golangci.yml` の全 `files:` ブロック (18 個) を確認 | **一致** — すべて `**/entity/**` `**/usecase/**` 等のパターンで、`router`/`auth`/`logger`/`constants`/`config`/`di` を対象にする `files:` は存在しない |
| 9 | **`layering-scopes.yml` の検査①に影響しない** (STRUCTURE.md:69-70) | `ci.yml:229` の①定義 (`service/ usecase/ repository/` 配下) | **一致** (`common_layers` は①から差し引くだけで、`config` は元々①に現れない) |
| 10 | `D-2④` が `controller/errresp.go` **だけ**を除外している (STRUCTURE.md:141) | `sed -n '201,210p' .github/workflows/ci.yml` | **一致** (`grep -v 'controller/errresp\.go'` の 1 本のみ) |
| 11 | `make golden` + `git diff --exit-code` が再生成漏れを落とす (STRUCTURE.md:150) | `ci.yml:88-100` | **一致** (`Makefile` に `golden:` が無ければ `::error` + exit 1) |
| 12 | §5 の 5 つの本数 (15 / 18 / 5 / 6 / 37) (STRUCTURE.md:195-198) | `docs/design/API/README.md:363-376` の総覧表 | **値は一致**。ただし検算対象外 (中 4) |

---

## 是正状況 (2026-08-03 21:30 時点。起票を受けた側が更新する欄)

| 指摘 | 状態 | 対応内容 / 残作業 |
|---|---|---|
| 重大 1 (「0 件」の事実誤り) | **一部実施** | `docs/design/architecture.md` §3.3 を訂正済み — 再現コマンド化 (DR-9)・**v2 からの逸脱**としての採用/却下案 (a)(b)・移植タスクの工数明記。**却下 (a)(b) の根拠は `usecase/{asset,idea,theme}` が v2 にも存在するパス衝突** (再現: `ls hassan-v2-backend/usecase/`) で、depguard の `files:` では移植分だけを除外できないこと。**残: `templates/app-monorepo/backend/STRUCTURE.md:111` の「0 件」削除 (中 5 の 3 重管理解消とセット)** |
| 重大 2 (`common/` が設計 SSOT に無い) | **実施済み** | `architecture.md` §3.5.1 に技術基盤 6 パッケージの `common/` 配下配置を**決定として追記** (却下案 (a) ルート直下に 6 個 / (b) `internal/`)。パス表記を §3.5.2 の区分表・§3.9④の対象パス・§3.2 の図・§3.3 の責務表・§3.5.1 の L-4 行で `common/config/**` `common/di/**` に統一。`layering-scopes.yml` の注記を整理し、`STRUCTURE.md` §1.1 を「決定の正は設計側」に修正 |
| 重大 3 (env キー集合照合 CI が無い) | **未対応** | `templates/app-monorepo/.github/workflows/ci.yml` に他 6 種と同形の未実装ガードステップを追加する。あわせて `architecture.md` §5 の D-5 行と `operations.md` §7.2-3 に検査 ID を振る |
| 重大 4 (立ち上げ手順に骨格が無い) | **未対応** | `templates/README.md` の手順に骨格 / `STRUCTURE.md` / `.golangci.yml` / `layering-scopes.yml` / `prompts/agents.yaml` のコピーを追記。`architecture.md` §7 の引き渡し行にも追加 |
| 中 1〜中 5 / 軽微 1〜7 | **未対応** | 中 3 (env の①インフラ由来/②アプリ由来の二分) と 中 5 (3 重管理の解消) は重大 1・3 と同じ箇所を触るため同時に行う |

**中断理由 (2026-08-03 21:26)**: 是正の途中で **`templates/` に並行編集が入った** —
`templates/app-monorepo/backend/CLAUDE.md.tmpl` の規約本文が新規ファイル
`templates/app-monorepo/backend/.claude/rules/05-architecture-coding-rules.md` へ移され、
`templates/README.md` / `STRUCTURE.md` §1.2 も更新された。**重大 3・4 と中 3・5 の是正先が
このファイル移動により変わった**ため、再構成の完了を待ってから続行する (ユーザー判断)。
**レビュー本文の指摘内容は有効** — 参照する行番号だけが再構成後にずれる。

---

## 重大 (Must Fix)

### 重大 1. 「v2 実測: `usecase/` から `controller/dto` を import しているファイルは 0 件」が事実誤り (実測 29 ファイル / 非テスト 27)

**事実**: `templates/app-monorepo/backend/STRUCTURE.md:104` は

> **v2 実測: `usecase/` から `controller/dto` を import しているファイルは 0 件**

と断定し、同じ主張が `docs/design/architecture.md:173`〜`:174` (「DTO は構造的に Controller 層に閉じている」)
にもある。実測は逆である:

```
$ cd /Users/yuyamorishita/aillio/hassan/hassan-v2-backend
$ grep -rl "controller/dto" usecase/ | wc -l
      29
$ grep -rl "controller/dto" usecase/ | grep -v "_test.go" | wc -l
      27
$ grep -rl "controller/dto" usecase/ | grep -v _test | cut -d/ -f2 | sort -u
account
asset
business_plan
idea
research
research_sheet
```

代表例: `hassan-v2-backend/usecase/asset/list_assets.go:7` / `usecase/account/list_accounts.go:7` /
`usecase/business_plan/converter.go:7` / `usecase/research/list_research_chats.go:7`。

**なぜ本番で問題になるか** (3 つ、いずれも実装リポに実費で出る):

1. **v3 の規約が「v2 踏襲」ではなく「v2 からの逸脱」である**。CLAUDE.md.tmpl:102-106 と STRUCTURE.md §1.3 は
   「(v2 踏襲)」の文脈で「DTO を UseCase / Service まで持ち込まない」を書いているが、v2 の 6 ドメイン・27 ファイルは
   まさにそれをやっている。ハイブリッド方針 (ルート `CLAUDE.md`) では**逸脱には却下案と理由が必要**であり、
   「v2 も既にこうなっている」という (誤った) 根拠で通すと、後から「なぜ v2 と違うのか」が説明できなくなる。
2. **移植 (§3.5.2 の「v2 移植分」) が depguard で必ず落ちる**。`templates/app-monorepo/backend/.golangci.yml:149`〜`:155` の
   `L1-usecase-no-controller` は `files: "**/usecase/**"` (= **移植分も含む全 usecase**) に対して
   `deny: pkg: "<module-path>/controller"` を掛ける。depguard の `pkg` は**前方一致**なので
   `<module-path>/controller/dto` も deny 対象になる。§3.5.2 は移植分を **L-2 / L-3 / L-5 と肥大化 lint の
   対象外**とするだけで **L-1 は全体に適用**すると決めているため、`account` / `business_plan` / `research` /
   `research_sheet` を移植した瞬間に 27 ファイル (テスト込み 29) が lint エラーになる。
   設計は「0 件だから問題は起きない」と言っているので、この工数はどのタスクにも積まれていない。
3. **誤事実が 3 箇所に同時増殖した** (`architecture.md` / `CLAUDE.md.tmpl` / `STRUCTURE.md`)。
   実装リポの開発者は雛形を検証せず転記する (`.claude/rules/04-review.md` の観点 1)。

**是正案**:

- STRUCTURE.md:104 と architecture.md:173-174 の「0 件」を**実測値に置き換え、再現コマンドを併記する**
  (DR-9 の「数えた値を書かずに再現コマンドを書く」に従い `grep -rl "controller/dto" usecase/` を書く)。
- 「DTO を UseCase / Service に持ち込まない」を **v2 からの明示的な逸脱**として書き直し、却下案
  (a) v2 現状踏襲 = 27 ファイルの現状を正とする / (b) v3 新規ドメインのみ適用 と理由を添える。
- **移植分の扱いを決める**: ①`L1-usecase-no-controller` の `deny` を `<module-path>/controller` から
  `dto` を除く形に分ける (移植分だけ許す) か、②移植時に Input 型へ書き換える (工数を移植タスクに積む)。
  どちらでも `.golangci.yml` と §3.5.2 の表・`layering-scopes.yml` を同一 PR で更新する。

### 重大 2. `common/` の導入が設計 SSOT に反映されておらず、「写しのドリフトを作らない」という自らの規約を破っている

**事実**:

| 場所 | 記述 |
|---|---|
| `templates/app-monorepo/backend/CLAUDE.md.tmpl:158` (本コミットで変更) | 共通層の対象パス = `entity/**` / `gateway/**` / `controller/**` / **`common/config/**`** |
| `templates/app-monorepo/backend/CLAUDE.md.tmpl:159` (同) | 配線 = **`common/di/**`** |
| `docs/design/architecture.md:340` (**未更新**) | 共通層の対象パス = `entity/**` / `gateway/**` / `controller/**` / **`config/**`** |
| `docs/design/architecture.md:159` (**未更新**) | 責務表の di 行のパッケージ = **`di/`** |
| `templates/app-monorepo/backend/layering-scopes.yml:48` | 「**architecture.md §3.5.2 の「共通層」の行と同一の集合に保つ** (写しのドリフトを作らない)」 |

`grep -rn "common/" docs/` の結果は `docs/analysis/v2-llm-inventory.md:311` (v2 の prompt ディレクトリ名) の
**1 件のみ** — **設計側に `common/` は一切存在しない**。

**なぜ本番で問題になるか**:

- **どちらが正か決まっていない**。`STRUCTURE.md:3-4` は「決定の正は設計リポ側」と宣言し、
  `layering-scopes.yml:48` は「§3.5.2 の行と同一に保つ」と宣言している。その両方に反して、
  雛形だけが `common/config/**` になった。実装者が architecture.md を読めばルート直下に `config/` を作り、
  STRUCTURE.md を読めば `common/config/` を作る。**この分岐は depguard の `files:` と CI 検査の
  除外パスに直結する** (D-2⑥ の `grep -vE '(^|/)config/'` は両方に当たるが、将来 `files: "**/config/**"` の
  規則を足す時に破綻する)。
- CLAUDE.md.tmpl 自身の運用ルール (:161-162) が「**本表に無いパッケージを新設する PR は、本表と
  `.golangci.yml` と `layering-scopes.yml` への追記を同一 PR に含める**」と定めている。本コミットは
  `common/{router,auth,logger,constants,config,di}` の **6 パッケージを新設**しながら、
  設計側の表 (架 §3.5.2) を更新していない。
- `common/` は「雛形固有の整理」(STRUCTURE.md:53) と説明されているが、**パッケージの物理配置は
  depguard の対象パス・CI 検査の除外パス・引き渡し時の依存順序に影響する設計判断**であり、
  雛形限りの裁量ではない (ルート `CLAUDE.md`: 「逸脱する場合は `docs/design/architecture.md` に理由と却下案を書く」)。

**なお、STRUCTURE.md §1.1 の 2 つの load-bearing な主張は真である** (照合 #8 / #9 で確認済み) —
問題は主張の真偽ではなく、**決定が設計 SSOT に無いまま雛形だけが動いたこと**。

**是正案**: `docs/design/architecture.md` §3.5.2 の表 (`config/**` → `common/config/**`)・§3.3 の di 行
(`di/` → `common/di/`)・§3.2 の図の読み方 3・4 と §3.5.1 の L-4 行の `di/` 表記を同一 PR で更新し、
**§3.5.1 の採用パッケージング (layer-first) の節に「技術基盤 6 パッケージを `common/` 配下に置く」決定と
却下案 (ルート直下に 6 個並べる / `internal/` にする) を追記する**。あわせて `layering-scopes.yml:50-53` の
注記を「設計側と同一表記になったので注記不要」に整理する (注記で差分を説明するのではなく、差分を消す)。

### 重大 3. `env/<env>.env` のキー集合照合 CI を「ある」と書いているが、雛形に存在せず「未実装なら落ちる」ガードも無い (D-5)

**事実**: 本コミットが `templates/app-monorepo/backend/CLAUDE.md.tmpl:257`〜`:259` に追記した文:

> 環境で変わる値 … は **非秘密なら `env/<env>.env` (git 管理・秘密を置かない・イメージに同梱)**、
> 秘密なら Secrets Manager … を **`config` が読む**。…
> **`env/<env>.env` のキー集合は `config` の非秘密環境値の定義と CI で照合する** (秘密の混入・未知キーで失敗)

設計側も同じ機構を要求している (`docs/design/operations.md:136` / `:751`、`docs/design/infrastructure.md:502`)。
しかし:

```
$ grep -rn "env/\|\.env\|secret" templates/app-monorepo/.github/workflows/ci.yml
(0 件 — env ファイルに触れるステップは存在しない)
```

`ci.yml` は他の未実装検査 6 種 (`check-route-auth.sh` / `check-owner-scope.sh` / `check-tool-contract.sh` /
`check-error-contract.sh` / `check-layer-scopes.sh` / `check-crypto-rand.sh` / `check-required-tests.sh`) には
**「未実装なら `::error` + exit 1」のガードステップを置いている** (`ci.yml:130`〜`:163`, `:222`〜`:240`, `:293`〜`:301`)
のに、env キー集合照合だけそれが無い。

**なぜ本番で問題になるか**: `env/<env>.env` は **git 管理かつ Docker イメージに同梱**する
(`operations.md:751` が `.dockerignore` から意図的に除外しないと明記)。これは v2 の最大の運用欠陥
(`operations.md:36` の「シークレットを `.env` に入れてイメージへ焼き込み dev/prod で同一イメージを共有」) と
**形式が同一で、秘密が入っていないことだけが違い**である。その唯一の防波堤が「CI で照合する」なのに、
機構が無く、無いことも検出されない。**`ANTHROPIC_API_KEY` を `env/prod.env` に書いた PR が緑で通る**。
D-5 (シークレット管理) は本番ゲートであり、「実装リポで作る」で足りるなら他 6 種と同じくガードが必要。

**是正案**: `templates/app-monorepo/.github/workflows/ci.yml` に

```yaml
- name: D-5 env ファイルのキー集合検査 (config の②アプリ由来定義との照合)
  run: |
    if [ -x scripts/check-env-keys.sh ]; then bash scripts/check-env-keys.sh
    else echo "::error::scripts/check-env-keys.sh が未実装です (設計: operations.md §3.3 の②)"; exit 1; fi
```

を追加する (他 6 種と同じ形)。あわせて `operations.md` §7.2 の立ち上げ手順 3 に検査 ID を振り、
`architecture.md` §5 の **D-5 行**にこの CI 検査を明記する (現状は保管先の決定しか書かれていない)。

### 重大 4. 本コミットの成果物 (骨格 41 ディレクトリ / STRUCTURE.md / agents.yaml) が `templates/README.md` の立ち上げ手順に載っていない — 手順どおりコピーすると届かない

**事実**: `templates/README.md:26`〜`:35` の立ち上げ手順がコピーするのは 4 つだけ:

```bash
cp -R templates/$R/.claude            <impl-repo>/
cp -R templates/$R/scripts            <impl-repo>/
cp -R templates/$R/.github            <impl-repo>/
cp    templates/$R/CLAUDE.md.tmpl     <impl-repo>/CLAUDE.md
```

`STRUCTURE.md` / `prompts/agents.yaml` / 骨格 41 ディレクトリ / `env/` / `.golangci.yml` /
`layering-scopes.yml` は**どれも含まれない**。
`grep -rn "STRUCTURE.md" docs/ aidlc-docs/ templates/ .claude/` のヒットは
`templates/app-monorepo/backend/layering-scopes.yml:50` と `templates/app-monorepo/backend/CLAUDE.md.tmpl:108` の
**雛形内の 2 件のみ** — 立ち上げ手順・設計書のどこからも参照されていない。
`docs/design/architecture.md:881` の引き渡し記述も
「`templates/` … (CLAUDE.md 雛形・実装/レビューエージェント・pre-commit・CI)」のままで、骨格に言及していない。

**なぜ本番で問題になるか**:

- 手順どおり立ち上げると `.golangci.yml` と `layering-scopes.yml` が無い状態で `ci.yml` が動く →
  `golangci-lint-action` は設定ファイル不在で失敗し、`D-2⑦` は `check-layer-scopes.sh` 未実装で exit 1。
  **CI が最初から赤で、原因が「コピーし忘れ」だと分からない** (`.golangci.yml` / `layering-scopes.yml` の
  漏れ自体は本コミット以前からの既存欠陥だが、本コミットで漏れる対象が 41 ディレクトリ + 2 ファイル増えた)。
- `STRUCTURE.md:75` は「**決定と立ち上げ手順の正は `../README.md`**」と書いているのに、
  その `../README.md` は `STRUCTURE.md` の存在を知らない (相互参照が片方向)。
- `.gitkeep` 41 件の運用ルール (STRUCTURE.md:7「最初のファイルを置いたら `.gitkeep` を消す」) が
  立ち上げ手順に現れないため、実装リポに `.gitkeep` が残り続ける。
  これは `.golangci.yml` の `files: "**/service/**"` 等には影響しないが、
  `check-layer-scopes.sh` の①「実ディレクトリ集合」の実装によっては `.gitkeep` だけの空ディレクトリを
  ドメインとして数えるか否かで挙動が変わる (実装リポ側の仕様が未定 = DR-5)。

**是正案**: `templates/README.md` の立ち上げ手順に

```bash
cp -R templates/app-monorepo/backend/{common,controller,usecase,service,repository,entity,gateway,db,prompts,env,testdata} <impl-repo>/
cp    templates/app-monorepo/backend/{STRUCTURE.md,.golangci.yml,layering-scopes.yml}                                     <impl-repo>/
```

相当を追記し、「`.gitkeep` は最初のファイルを置いた時に削除する」「`check-layer-scopes.sh` の①は
`.gitkeep` のみのディレクトリをどう扱うかを実装リポで決める」を明記する。
`docs/design/architecture.md` §7 の引き渡し行にも骨格と `STRUCTURE.md` を加える (DR-8: 機構を足したら
その機構を語る文書を同じ差分で直す)。

---

## 中 (Should Fix)

### 中 1. `agents.yaml` の `diverge` / `plan` の `tools: []` が「無言で緑」になる — P-4 の `web_search` を BE-9 で落とす経路

**事実**: `templates/app-monorepo/backend/prompts/agents.yaml:26` (orchestrator) には
「**空のままだと `check-tool-contract.sh` が実ハンドラとの不一致で落ちる (無言で緑にしない)**」という注記があるが、
`:31` (diverge) と `:36` (plan) の `tools: []` には注記が無い。
一方、設計は **P-4 (plan) が `web_search` を使う**ことを 4 箇所で前提にしている:
`docs/design/API/plans.md:378` / `:414` / `:445` / `:635`、および `docs/design/operations.md:322`
(「承認材料に置き換わる `Tools` の一覧を出し、**`web_search` 等の既存ツールの欠落を人間が目視で確認する**」)。

**なぜ本番で問題になるか** — どちらに倒しても壊れる:

- **列挙しない場合**: `agents.yaml` が発行対象の SSOT (`operations.md:299`「発行コマンドが実際に送る
  prompt / tool の集合と列挙の一致」) なので、`tools: []` のまま発行すると **`Tools` は全置換 (BE-9)** で
  `web_search` が消える。しかも検査②は「発行が送る集合 == 列挙」を見るだけなので**両方空で一致 = 緑**。
  企画書の Web リサーチ (V-11 を P-4 に統合した経路 — `plans.md:635`) が黙って死ぬ。
- **列挙する場合**: `architecture.md:584`〜`:585` の検査 1 / 2 は「schema にある tool 名に**ハンドラが無い**
  → **起動失敗**」「ハンドラがあるのに schema に無い → 同上」であり、**サーバ側ツール (Anthropic 提供の
  `web_search`) の例外が書かれていない**。列挙すると起動時チェックで落ちる。

**是正案**: ①`architecture.md` §3.8.4 の検査 1 / 2 に「**Anthropic 提供のサーバ側ツールはローカルハンドラを
持たないため対象外とし、`agents.yaml` で `kind: server` を明示する**」を足す。
②`agents.yaml` の `diverge` / `plan` に「P-4 は `web_search` を使う (plans.md §…)。`Tools` は全置換なので
サーバ側ツールも必ず列挙する」旨のコメントと `kind` フィールドを入れる。
③diverge がサーバ側ツールを使うかは**未調査** — 判定して同じ形で書く。

### 中 2. STRUCTURE.md が CI 検査 `D-2⑦` の適用範囲を過大に書いている (`db/queries` は走査対象外・条件 1 の右辺が誤り)

**事実**: 3 箇所で `D-2⑦` の効力を実際より広く書いている。

| 箇所 | 記述 | 実際 |
|---|---|---|
| `STRUCTURE.md:9` | 「本ファイルに無い**パッケージを新設**するときは §4 の手順を踏む (登録漏れは CI の `D-2⑦` が落とす)」 | ①は `service/` `usecase/` `repository/` 配下の**2 段目ディレクトリ**だけ (`ci.yml:229`)。`gateway/<新規>` や `common/<新規>` の新設は検出されない |
| `STRUCTURE.md:120-121` | 「`usecase/` `service/` `repository/` **`db/queries/`** に置くドメインは `layering-scopes.yml` の **`v3_domains` と完全に一致**させる (CI 検査 `D-2⑦` の条件 1)」 | (a) `db/queries/` は①の走査対象に含まれない (b) 条件 1 の右辺は **`v3_domains` ∪ `ported_domains`** (`ci.yml:235` / `architecture.md:366`) |
| `STRUCTURE.md:191` | 「未登録のまま作ると `D-2⑦` の条件 1 で落ちる」(§5 の未作成ディレクトリ = `knowledge` / `board` / `news` / `settings` / `account` / `ops`) | `db/queries/<domain>/` だけを作る場合は落ちない |

**なぜ本番で問題になるか**: **矛盾ループが実際に起きる**。`docs/design/data-model.md:281`〜`:296` (DM-18) は
`db/queries/account/` → `db/rdb/account` を**設計として確定**している (v2 移植分)。
STRUCTURE.md:121 を字面どおり守ると `account` を `v3_domains` に入れるしかなく、そうすると条件 2
(③ == `v3_domains`) が `.golangci.yml` への登録を要求し、条件 3 (移植分は③に現れてはいけない) と
§3.5.2 の「移植分は L-2 / L-3 / L-5 の対象外」に**同時に違反する**。
`STRUCTURE.md:178`〜`:181` (§4) は正しく `ported_domains` を案内しているので、**§2 と §4 が食い違っている**。
加えて DR-6 の「検査が対象 0 件を検査して緑になる」型の誤解を生む (「D-2⑦ があるから登録漏れは起きない」)。

**是正案**: §2 の文を「`usecase/` `service/` `repository/` に置くドメインは `v3_domains` ∪ `ported_domains` と
一致させる (D-2⑦ の条件 1)。**`db/queries/` は D-2⑦ の走査対象ではない** — 対応は
`data-model.md` §3.6 と `sqlc.yaml` が正で、機械検査は無い」に直す。§5 の「落ちる」も
`usecase/` / `service/` / `repository/` を作った場合に限定する。§1 の :9 も同様に範囲を限定する。

### 中 3. 非秘密環境値の①インフラ由来 / ②アプリ由来の二分が CLAUDE.md.tmpl の追記で潰れている (D-1)

**事実**: 本コミットの追記 (`CLAUDE.md.tmpl:257`) は

> 環境で変わる値 (**使用モデル・エンドポイント**・ログレベル) は **非秘密なら `env/<env>.env`** …

と書くが、設計 (`docs/design/operations.md:118`) は非秘密値を**値の出所で 2 つに分けている**:

- **インフラ由来** (RDS ホスト名・S3 バケット名・ロググループ名) → **ECS タスク定義の `environment`**
  (`stacks/<env>/` の ecspresso テンプレート。infra の PR → `apply`)
- **アプリ由来** (既定モデル名・フィーチャーフラグ) → **backend の `env/<env>.env`** (backend の PR → デプロイ)

「エンドポイント」は典型的にインフラ由来 (RDS / S3 / ロググループ) であり、`env/<env>.env` に置く対象ではない。
また `APP_ENV` は「**タスク定義側に残す唯一のアプリ由来値**」(同:118) という例外があり、これも落ちている。

**なぜ本番で問題になるか**: 変更経路 (誰の PR で・誰が apply するか) が値ごとに違うのに、
雛形が 1 つに潰しているため、実装者が RDS ホスト名を git 管理の `env/prod.env` に書く。
そうなると **infra の `terraform apply` で RDS を再作成したときに backend の env ファイルが追随せず**、
かつ「インフラの値が backend リポに二重管理される」状態になる (設計が却下案 (c)(e) として明示的に退けた形)。
CI のキー集合照合 (重大 3) が無いので検出されない。

**是正案**: 追記を「**非秘密は 2 つに分かれる** — インフラ由来 (RDS ホスト名・バケット名等) は
ECS タスク定義の `environment`、アプリ由来 (既定モデル名・フィーチャーフラグ) は `env/<env>.env`。
`APP_ENV` は例外でタスク定義側。秘密は Secrets Manager。**5 分類の SSOT は `operations.md` §3.3**」に直す。
`STRUCTURE.md:47` の `env/` 行にも「アプリ由来のみ」を明記する。

### 中 4. STRUCTURE.md §5 が 5 つのエンドポイント本数を転記したが、検算対象に入っていない (DR-9 の再発余地)

**事実**: `STRUCTURE.md:195`〜`:198` が `knowledge` 15 本 / `board` 18 本 / `news` 5 本 / `settings` 6 本 /
`account` 37 本を書いている。値は `docs/design/API/README.md:363`〜`:376` の総覧 (SSOT) と**一致している**
(照合 #12)。しかし:

```
$ grep -n "templates\|STRUCTURE" scripts/check-endpoint-mapping.sh scripts/check-table-counts.sh scripts/doc-lint.sh
(0 件)
```

`make check-endpoint-mapping` は `templates/` を一切見ない (照合 36 件はすべて `docs/design/API/` 内)。
つまり**総覧の本数が変わっても STRUCTURE.md は無言でずれる**。
`.claude/rules/feedback_review_patterns.md` の DR-9 は「新しく「N 件」を書くときは、**同時に検算の対象に
加えるか、書かずに定義元へのリンクにする**」と定めており、同一コミットの
`templates/app-monorepo/frontend/CLAUDE.md.tmpl:54` は「**ここに件数を転記しない = DR-9**」と明記して守っている。
**同じコミット内で規約の遵守と違反が併存している**。

**是正案**: §5 の本数を落とし「本数は `docs/design/API/README.md` §3 の総覧が正」へのリンクに置き換える
(雛形は実装リポへコピーされるため設計リポへの相対リンクは張れない → **本数を書かないのが唯一の解**)。
本数を残すなら `check-endpoint-mapping.sh` の走査対象に `templates/app-monorepo/backend/STRUCTURE.md` を加え、
故障注入 (総覧の 15 → 14 に改ざんして exit 1 になること) で検出力を確認する。

### 中 5. 「同じ表を二重に持たない」と宣言しながら §1.3 が 3 重管理になっている (誤事実が同時に 3 箇所へ増殖した)

**事実**: `STRUCTURE.md:4`〜`:5` は

> 責務・禁止事項・依存規則の本文は `CLAUDE.md` に持たせてある。**同じ表を二重に持たない**

と宣言している。しかし本コミットは **同一内容を 2 箇所に同時追加**した:

| 内容 | 場所 1 | 場所 2 | 場所 3 (既存) |
|---|---|---|---|
| DTO / Input の置き場所と「DTO を UseCase に持ち込まない」 | `STRUCTURE.md:89`〜`:108` (表 3 行) | `CLAUDE.md.tmpl:102`〜`:108` | `architecture.md:167`〜`:178` |
| バリデーション責務分離 + v2 実例 (`dto/account.go:25` / `entity/account.go:44`) | `STRUCTURE.md:110`〜`:116` | `CLAUDE.md.tmpl:110`〜`:120` | `architecture.md:180`〜`:198` (表) |

**なぜ本番で問題になるか**: 重大 1 の誤事実 (「0 件」) が、この 3 重管理によって**1 コミットで 2 箇所増えた**
(architecture.md → STRUCTURE.md + CLAUDE.md.tmpl)。DR-8 (修正の波及漏れ) は次の修正時に必ず起きる:
片方だけ直すと残りが旧記述のまま実装リポへコピーされる。

**是正案**: `STRUCTURE.md §1.3` を**表 (置き場所の索引) のみ**に削り、バリデーション責務分離の本文は
`CLAUDE.md` (雛形) 側 1 箇所に寄せる。v2 実例の パス:行 は**設計 (architecture.md §3.3) の 1 箇所だけ**に置き、
雛形からは「詳細は設計リポ `architecture.md` §3.3」参照に統一する (雛形 → 設計リポの参照は
`CLAUDE.md.tmpl:8` の `<hassan_v3 の絶対パス>` プレースホルダで既に成立している)。

---

## 軽微 (Nice to Have)

1. **`STRUCTURE.md` のツリーに設計側が要求するファイルが 3 つ欠けている** — `sqlc.yaml`
   (§4 の :185 が「`sqlc.yaml` の出力設定も同じ PR で更新する」と参照している)、`.dockerignore`
   (`operations.md:751` が `.env*` / `*.pem` の除外を要求)、`.env.example` (同 :748 がキー名のみの配置を要求)。
   ツリーが「置き場の索引」を名乗るなら 3 行足す。
2. **`env/<env>.env` の実ファイル名形が未確定** — v2 は `env/.dev.env` (ドット始まり。`operations.md:37`)、
   v3 の設計と雛形は `env/<env>.env` 表記のみ。`env/dev.env` なら `.dockerignore` の `.env*` に当たらず同梱され、
   `env/.dev.env` なら**除外されてイメージから消える**。`STRUCTURE.md:47` で実ファイル名を確定させる (DR-5)。
3. **`common/logger/` の「v2 踏襲」が条件を落としている** — `docs/design/observability.md:444` は
   「ログ基盤: `hassan-v2-backend/logger/logger.go` (**形式は踏襲。登録条件は踏襲しない**)」。
   `STRUCTURE.md:28` の「構造化ログ (v2 踏襲。O-1)」だけを読むと v2 の登録条件まで持ち込む。
4. **`CLAUDE.md.tmpl:136` の `IdeaBoard.CanEdit(accountID)` に パス:行 が無い** (DR-1)。
   設計側は `architecture.md:233` が `hassan-v2-backend/entity/idea_board.go:113`〜`:118` を持っている
   (照合 #6 で正確と確認済み) ので、そのまま引く。
5. **`layering-scopes.yml` の `common_layers` は実質 no-op** — 本コミットの注記 (:50-53) が
   「`config` は①に現れないため影響しない」と明言したことで、:49 の「検査 ⑦ はこのリストを①のドメイン集合から
   除外するために使う」が**一度も発火しない設定**であることが明示された。リストの存在意義を整理するか、
   「将来 `usecase/config` 等が作られた場合の保険」と目的を書く。
6. **ツリーの `CLAUDE.md.tmpl` 行 (:20)** — 実装リポのツリーを描いているなら `CLAUDE.md`。
   雛形のファイル名と実装リポのファイル名が同じツリーに混在している。
7. **`CLAUDE.md.tmpl` は `*.md` でないため `doc-lint` の対象外** — 本コミットで最も内容が増えたファイルが
   リンク切れ・TODO 残の検査を受けない。`doc-lint.sh:30` の `find` に `-o -name '*.md.tmpl'` を足すか、
   対象外であることを `.claude/rules/05-harness.md` の「見る / 見ない」表に明記する。

---

## 本番観点カバレッジ (`.claude/rules/08-production-gates.md`)

本差分が触れる範囲についての評価。「該当なし」は本差分が扱う対象ではなく、設計側 (`docs/design/`) が回答を持つもの。

| ID | 状態 | 箇所 |
|---|---|---|
| A-1 | 回答あり | `STRUCTURE.md:27` (`common/auth/` = JWT 検証・ミドルウェア。v2 踏襲) / `ci.yml:130` の `check-route-auth.sh` ガード |
| A-2 | 該当なし (設計側: `auth.md` §6.2 / §6.7) | — |
| A-3 | 該当なし (設計側: `data-model.md` §4) | — |
| A-4 | 回答あり | `STRUCTURE.md:142` (`send_message.go` で所有者スコープ確定) / `ci.yml:140` の `check-owner-scope.sh` ガード |
| A-5 | 該当なし (設計側: `API/README.md` D-API-6 / `auth.md` §6.6) | — |
| **A-6** | **回答あり** | `STRUCTURE.md:143` = `usecase/conversation/tool_registry.go` を「**A-6 の束縛点**」として名前固定。`architecture.md:539` (束縛は 1 箇所のみ) と一致。骨格に `usecase/conversation/` が実在 |
| A-7 | 該当なし (設計側: `API/README.md` D-API-8') | — |
| **O-1** | 回答あり | `STRUCTURE.md:28` (`common/logger/`)。ただし軽微 3 の条件落ち |
| **O-2** | **回答あり** | `STRUCTURE.md:37` / `:146` = `gateway/anthropic/{session,stream}.go` を「**全 LLM 呼び出しの単一関門**」として名前固定。`gateway/` の 5 パッケージ (anthropic / exa / gemini / mail / s3) は設計の LM-D (`llm-migration.md:112`) と一致し、**埋め込みプロバイダ未選定のためディレクトリ未作成であることも明記** (`:162`)。LLM を呼ぶ経路が gateway 外に漏れていない |
| O-3 | 回答あり | `STRUCTURE.md:145` (`runner.go` に安全弁)。上限超過時の挙動は設計側 (`observability.md`) |
| O-4 | 回答あり | `ci.yml:151` の `check-tool-contract.sh` ガード (BE-8 / BE-12) |
| O-5 | 該当なし (設計側: `observability.md` §4.4) | — |
| O-6 | 回答あり | `ci.yml:242` の D-2⑧ (監査ログ戻り値の `_ =` 破棄検出) |
| O-7 | 該当なし (設計側: `observability.md` §6) | — |
| **D-1** | **回答に欠陥** | `STRUCTURE.md:47` / `CLAUDE.md.tmpl:257`。**中 3** — インフラ由来 / アプリ由来の二分が潰れている |
| **D-2** | **回答あり (検証済み)** | `STRUCTURE.md:9` / `:141` / `:150` が参照する `D-2④` `D-2⑦` は `ci.yml:201` / `:222` に実在し内容も一致 (照合 #10 / #11)。ただし**中 2** で適用範囲の過大表現 |
| D-3 | 該当なし (設計側: `operations.md` §5) | — |
| D-4 | 回答あり (未確定を明記) | `STRUCTURE.md:41` 「適用方式は D-4 で確定してから」 |
| **D-5** | **未回答 (機構が無い)** | **重大 3** — 「CI で照合する」と書いてあるが `ci.yml` に該当ステップもガードも無い |
| **D-6** | **回答あり (欠陥あり)** | `prompts/agents.yaml` が再発行トリガの SSOT として成立 (`operations.md:299` / `llm-migration.md:561` と一致。`post_diverge_chat` を列挙しない旨も明記 = `llm-migration.md:566` と一致)。ただし**中 1** — `plan` / `diverge` の `tools: []` が無言で緑 |
| D-7 | 該当なし (設計側: `operations.md` §6) | — |
| D-8 | 該当なし (infra リポ側) | — |

## 頻出パターン (`.claude/rules/feedback_review_patterns.md`) の確認結果

| # | 判定 |
|---|---|
| DR-1 | **該当あり (重大 1)** — 「v2 実測 0 件」が誤り。軽微 4 (出典なしの v2 実例) |
| DR-2 | **該当あり (重大 3)** — D-5 が「CI で照合する」と書かれたまま機構が無い |
| DR-3 | 該当なし (本差分は既存データに触れない) |
| DR-4 | 該当なし — PoC の構造 (net/http・手書き store) の持ち込みは無い。骨格は v2 規約 + 6 層で一貫 |
| DR-5 | **該当あり (軽微 2)** — `env/<env>.env` の実ファイル名が未確定。他に「適切に」「必要に応じて」の類は無い |
| DR-6 | 該当なし — `make check-traceability` は 86/86 カバー。本差分は AC を増やしていない |
| DR-7 | 該当なし (プロトタイプ参照は無い) |
| **DR-8** | **該当あり (重大 2 / 重大 4)** — `common/` 導入と骨格追加が設計 SSOT (`architecture.md` §3.5.2 / §3.3 / §7) と `templates/README.md` に波及していない |
| **DR-9** | **該当あり (中 4)** — §5 の 5 本数が検算対象外 |
| BE-2 | 対応済み — `CLAUDE.md.tmpl:253`〜`:260` が設定値の SSOT を `config` 1 箇所に固定。`ci.yml:212` の D-2⑥ が `os.Getenv` を機械強制 (`common/config/` でも `(^\|/)config/` に当たるため機能する) |
| BE-8 | 対応済み (機構は未実装ガード付き) — `ci.yml:151` |
| **BE-9** | **不十分 (中 1)** — 全置換の注意書きは `agents.yaml:14` にあるが、`plan` の `web_search` を落とす具体的経路が塞がれていない |
| BE-10 | 対応済み — `STRUCTURE.md:145` (Runner が台帳への write-through を持つ) / `architecture.md:503` |
| BE-12 | 対応済み — `entity/toolresult/` (`STRUCTURE.md:39` / `:147`) と `testdata/golden/toolresult/` (`:49` / `:150`) が骨格に実在し、`testing.md:92` / `:221` の決定と一致 |

## 良かった点

- **§3「名前が設計で固定されているファイル」表の 10 行すべてが設計側に根拠を持ち、抜き取り照合でズレが無かった**
  (`send_message.go` / `tool_registry.go` / `tx.go` / `runner.go` = `architecture.md:475`〜`:506`、
  `gateway/anthropic/{session,stream}.go` = `:508`〜`:513`、`entity/toolresult/` = §3.8.5、
  `controller/errresp.go` = `ci.yml:204`、`testdata/golden/toolresult/` = `testing.md:221`)。
  **CI 検査の除外パスと設計の参照先を「名前を変えたら何が壊れるか」まで書いている**のは、
  実装リポで最も事故が起きる箇所を先に潰している。
- **`common/` 導入の影響について、検証可能な 2 つの主張に絞って書いている** (`.golangci.yml` 変更不要 /
  検査①に影響しない)。**両方とも実ファイルを読んで真であることを確認できた** (照合 #8 / #9)。
  「影響しないと思う」ではなく検査の走査対象を特定して書いているため、レビューが 5 分で終わる形になっている。
- **`agents.yaml` に「`post_diverge_chat` を列挙しない (存在しない Agent の再発行が走る)」を
  ネガティブリストとして書いている** — `llm-migration.md:566` の決定が雛形まで到達している。
  D-6 の「コードだけデプロイして Agent 再発行を忘れる」の逆 (消えた Agent を再発行し続ける) を先に塞いでいる。
- **§5「まだ作っていないディレクトリ」を保留理由付きで明示している** — 「無いこと」を意図として書いた点は
  DR-2 (無言の省略) の逆であり、`ops` (`llm_call_records` / `audit_logs` / レート制限) が
  `data-model.md:295` の `db/queries/ops/` と正確に対応している。
- **`.gitkeep` の削除タイミング (:7) と「本ファイルに無いパッケージの新設手順」(§4 の 3 点セット) を
  先に書いている** — 骨格を配るだけでなく、骨格を壊す操作の手順を同梱している。

## 判定

**是正が必要 (push 前に重大 4 件の解消を求める)**。最小の是正リスト:

1. **重大 1**: `STRUCTURE.md:104` / `architecture.md:173-174` の「0 件」を実測 (29 ファイル / 非テスト 27) へ訂正し、
   再現コマンドを併記。「v2 踏襲」→「v2 からの逸脱 (却下案付き)」に書き換え、
   移植分の depguard 扱い (`L1-usecase-no-controller` の deny 範囲) を決める
2. **重大 2**: `architecture.md` §3.5.2 の表・§3.3 の di 行・§3.5.1 に `common/` を反映 (決定 + 却下案)
3. **重大 3**: `ci.yml` に env キー集合検査の「未実装なら落ちる」ステップを追加
4. **重大 4**: `templates/README.md` の立ち上げ手順に骨格・`STRUCTURE.md`・`agents.yaml`・
   `.golangci.yml`・`layering-scopes.yml` のコピーを追記し、`architecture.md` §7 の引き渡し行を更新

中 1〜5 は同じ差分で直すのが安い (いずれも 1〜数行)。是正後の再レビューは本ファイルに追記する。

## 未調査 / 要確認

- **depguard の `deny.pkg` が前方一致であること**は depguard v2 の仕様理解に基づく (重大 1 の帰結 2)。
  実装リポで `usecase/x` から `controller/dto` を import する故障注入を 1 回行って確認すること。
  **前方一致でなかった場合も「0 件」が誤りであること自体は変わらない**。
- **P-2 (diverge) がサーバ側ツール (`web_search` 等) を使うか**は未調査 (中 1 の③)。
  P-4 については `plans.md:378` / `:414` / `:445` / `:635` で使用が確定している。
- `check-layer-scopes.sh` の①が「`.gitkeep` のみの空ディレクトリ」をドメインとして数えるかは
  スクリプト本体が未実装のため判定不能 (重大 4 の帰結)。
- `docs/` `aidlc-docs/` の**未コミット差分そのものはスコープ外**とした (依頼どおり)。
  ただし重大 1 / 重大 2 は設計側の同時修正が無いと閉じない。
