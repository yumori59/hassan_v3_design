# レビュー結果: `docs/design/auth.md` 5 巡目 (2026-07-31)

> レビュアー: `design-reviewer` (別セッション・第三者視点・**本番基準**)
> 範囲: 4 巡目の反映 (`review-auth-hardening.md` §15) で新規に入った / 拡張された設計判断の妥当性
> + `review-auth-api.md` の**未解決 重大 3 / 重大 7 の決着材料** + DR-8 全数チェック + §10.2 R-1〜R-5 の状態実測。
>
> **判定: 重大 6 件 / 中 7 件 / 軽微 5 件 — auth.md スコープの Design Freeze は不可**。
> 内訳の性質: ①**4 巡目で採った「MFA を主たる防御にする」決定に、試行回数の上限がどこにも無い** (v2 の
> 欠陥をそのまま継承。全契約横断の解除 API が 6 桁の総当たりで抜ける) ②**MFA 登録前の窓が
> パスワードのみで守られ、その初期パスワードは §5-8 が「深刻」と認定した v2 の math/rand トークンで
> 設定されたもの** ③**A-7 の判断を持つ文書が 1 つも無くなった** (architecture.md が取り下げ、auth.md は
> 「対象外」、下流 5 文書は具体設計を持つ) ④`review-auth-api.md` の重大 3 が 2 巡連続未対応
> ⑤除外リスト件数が 3 文書で 8/9 に分裂し **CI 期待値がずれている** (DR-9) ⑥書き込み系クエリが
> CI 検査の外にあり、§6.9 のテナント検証が機械強制されない。
> **①②⑥は「決定した防御に実行主体・上限・検査が付いていない」= BE-10 の 6 度目**である。

---

## 0. レビュー対象 (リポジトリ相対パス。全件)

### 0.1 主対象 (全文を通読)

| ファイル | 読んだ範囲 |
|---|---|
| `docs/design/auth.md` | **全文 1266 行** (§0 位置づけ / §1.1〜§1.7 / §2.1〜§2.3 / §3 / §4 / §5-1〜§5-11 / §6 前提・§6.1〜§6.11 / §7 / §8 / §9.1〜§9.3 / §10.1〜§10.2)。重点: **§6.2 / §6.3 / §6.4 / §6.6 / §6.7 / §9 / §10** |

### 0.2 整合を確認した相手 (該当節)

| ファイル | 読んだ範囲 |
|---|---|
| `docs/design/data-model.md` | `:43` (P-4) / `:64` (F-11) / `:94`,`:105` (§1.4 対応表) / `:125` (DM-9) / `:193`〜`:194` (§3.3 CI 検査①②) / `:363`〜`:398` (§4.1.2 の 2 表) / `:404`〜`:417` (§4.2) / `:816`,`:820` (§7 A-3 / A-7) / `:900` / `:930`〜`:1027` (§6.4 / §6.5 = DM-A3 / DM-A4) / `:1061` (§7.2 検査 1) / `:1109` / `:1136`〜`:1137` (R-DM-4 / R-DM-5) |
| `docs/design/architecture.md` | `:764` (§4 所有者列と除外リスト) / `:788` (§5 A-4 行) / `:791` (§5 A-7 行) |
| `docs/design/API/README.md` | `:78`〜`:79` (F-15 / F-16) / `:95` / `:110` (D-API-3) / `:116` (D-API-8') / `:158` (§1.4 CI 検査) / `:176` / `:185`〜`:193` (§2.2) / `:245`〜`:250` (§2.5) / `:346` / `:461`,`:466` (§4 A-2 / A-7) / `:483`〜`:484` (API-Q2 / API-Q3) |
| `docs/design/frontend.md` | `:979` (A-7 行)。**FE-Q7 (§16 の管理者経路 IP 制限) は 3 巡目で既指摘のため再指摘しない** |
| `docs/design/API/themes.md` | `:25` / `:94`〜`:96` (D-TH-3 / D-TH-5) / `:123`〜`:139` (§3.2 TM-1) / `:153` (A-7) |
| `docs/design/API/assets.md` | `:102` (D-AS-4) / `:133`〜`:146` (§3.2 AS-M1) / `:164` (A-7) / `:178`〜`:179` (AS-Q5 / **AS-Q11**) |
| `docs/design/API/idea-boards.md` | `:26` / `:182` (D-IB-11) / `:192` (§3.1) / `:310` (A-7) / `:322` (IB-Q3) |
| `docs/design/API/settings.md` | `:11`〜`:14` / `:42` / `:89`〜`:91` / `:109`〜`:132` (D-ST-1' / D-ST-3 / D-ST-7) / `:144`〜`:146` / `:182` / `:199` (§6 A-2) / `:219` (ST-Q5) |
| `docs/design/observability.md` | `:197` (握り潰し禁止の記述) |
| `docs/design/operations.md` | `:458` / `:822` (`sharing_settings` 移行タイミング) |
| `docs/analysis/v2-auth-tenancy.md` | `:83` / `:174` / `:221`〜`:223` / `:240` / `:270` |
| `aidlc-docs/inception/productionization/requirements.md` | `:46`〜`:59` (AC-1.1 / AC-1.2) |
| `aidlc-docs/inception/productionization/plan.md` | `:21` (AC-1.1 欄) / `:109` / `:180` / `:231` |
| `docs/design/API/auth-accounts.md` | **内容レビューは別レビュアー担当のため行わない**。auth.md 側から見た矛盾の確認のためのみ参照: `:163` / `:183` / `:271`〜`:274` / `:303`〜`:314` (AA-D-6 / AA-D-9 / **AA-D-10** / AA-D-15) / `:338` / `:425`〜`:462` (§3.5 許可リスト割り当て) / `:516`,`:564` (A-7) / `:530`〜`:600` (§5 R-AA-1〜13 / §6.1 AA-Q1〜Q3) |

### 0.3 既存レビュー (再指摘回避のため読了)

`aidlc-docs/reviews/productionization/review-auth-hardening.md` (§1〜§15。4 巡分) /
`aidlc-docs/reviews/productionization/review-auth-api.md` (§2 の重大 1〜7・§3 の中 1〜10・§7 再レビュー条件) /
`aidlc-docs/reviews/productionization/review-round3.md` (auth.md 該当部)

### 0.4 一次ソース (読み取り専用。**編集していない**)

`/Users/yuyamorishita/aillio/hassan/hassan-v2-backend` — `auth/` `router/` `controller/` `usecase/` `db/` `util/` `entity/`

---

## 1. 実行した検証

### 1.1 `make check` の生出力 (2026-07-31)

```
[WARN ] ./.claude/rules/05-harness.md:23 未確定マーカー: 23:| TODO / TBD / FIXME の残存 | 設計上の抜け漏れ |
[WARN ] ./.claude/rules/05-harness.md:56 未確定マーカー: 56:  自動的に WARN へ降格する (リンク切れ・`[Answer]` 未回答・TODO の検出は有効)
[WARN ] ./aidlc-docs/inception/productionization/plan.md:159 未確定マーカー: TODO に対応する確認リストを提供
[WARN ] ./aidlc-docs/inception/productionization/questions.md:174 未確定マーカー: (memo の TODO)
[WARN ] ./aidlc-docs/reviews/productionization/review-auth-accounts.md:11 / :372 未確定マーカー
[WARN ] ./aidlc-docs/reviews/productionization/review-layering.md:63 / :77 / :266 / :646 / :1002 未確定マーカー
[WARN ] ./aidlc-docs/reviews/productionization/review-operations-infrastructure.md:489 / :560 / :785 未確定マーカー
[WARN ] ./aidlc-docs/reviews/productionization/review-round3.md:72 未確定マーカー
[WARN ] ./CLAUDE.md:40 未確定マーカー
[WARN ] ./docs/analysis/gap-analysis.md:197 / :255 未確定マーカー
[WARN ] ./docs/design/data-model.md:954 未回答の [Answer]:
[WARN ] ./docs/design/design_memo.md:13,15,20,24,29,51,150 未確定マーカー
[WARN ] ./docs/design/frontend.md:1199 未回答の [Answer]:
[WARN ] ./docs/design/infrastructure.md:519 未回答の [Answer]:
[WARN ] ./docs/design/infrastructure.md:535 未回答の [Answer]:
[WARN ] ./docs/design/llm-migration.md:764 未回答の [Answer]:
[WARN ] ./docs/design/operations.md:695 未回答の [Answer]:
[doc-lint] 対象 89 ファイル / エラー 0 件 / 警告 31 件
[traceability] construction-workflow: 24/24 カバー — OK
[traceability] productionization: 47/47 カバー — OK
[traceability] 照合 2 feature / 未カバーあり 0 feature
[workflow-shell] 検査 52 ブロック / エラー 0 件
[table-counts] 実測: 機能テーブル 40 (個人 32 / 契約 8) / 分類 ①29 ②2 ③1
[table-counts] 照合 22 件 / エラー 0 件
```

**評価**: **エラー 0 / AC 未カバー 0**。`auth.md` に起因する警告は **0 件** (未回答 `[Answer]` は
infrastructure / llm-migration / operations / frontend / data-model §6.4 の 5 文書のもので、
**auth.md には未回答 `[Answer]` が無い** = §9.2 の主張と一致)。
**ただし `make check` はこのレビューの重大 4 (除外リスト件数の 8/9 分裂) を検出していない** —
`scripts/check-table-counts.sh:107`〜`:113` が auth.md について見るのは
**機能テーブル数 (40) / 個人境界 (32) / 契約境界 (8) の 3 値だけ**で、**除外リストの件数は検算対象外**。
これは DR-9 の運用注記「新しく増えた『N 件』が検算の対象に入っているか」に対する **未達 1 件**である
(重大 4 で扱う)。

### 1.2 一次ソースでの抜き取り照合 (14 件。うち結論を左右するもの 5 件)

| # | auth.md の記述 | 一次ソース | 結果 |
|---|---|---|---|
| 1 | `:526` `admin_accounts` に `last_locked_at` / `failed_sign_in_attempts` のカラムが存在しない | `hassan-v2-backend/db/schema.sql:52`〜`:64` | **一致**。列は `id`/`name`/`email`/`auth_role_id`/`admin_auth_role_id`/`crypted_password`/`language_type`/timestamps の 8 列で、ロック列・MFA 列は無い |
| 2 | `:533` `account_mfa_configs.account_id` は `accounts` への FK なので `admin_accounts` に使えない | `同:68`〜`:77` | **一致**。`PRIMARY KEY (account_id, mfa_type)` + `FOREIGN KEY (account_id) REFERENCES accounts (id) ON DELETE CASCADE` |
| 3 | `:534` 管理者トークンのクレームは `uid` + `exp` のみ | `hassan-v2-backend/auth/client.go:92`〜`:95` | **一致**。`jwt.MapClaims{"uid":…, "exp":…}` のみ |
| 4 | `:509` `POST /admin/signin` は公開 (`router.go:195`) | `hassan-v2-backend/router/router.go:194`〜`:195` | **一致** (`:194` が `r.Group("/admin")`、`:195` が実体。4 巡目 L1 の訂正が反映済み) |
| 5 | `:511`,`:546` MFA リセットの移植元は `POST /accounts/mfa/reset` (`router.go:83`。契約管理者が実行) | `同:83` → `usecase/account/reset_member_mfa.go:44`〜`:73` | **一致**。ロール確認 (`:46`) + **対象 ID 指定** + **同一 contract 検証 (`:60`〜`:62`。不一致は `AccountNotFound`)** |
| 6 | `:512` 管理者投入は v2 の `router.go:206` (`CreateAdminAccount`) + `:199-200` (公開のパスワード登録) | `同:206`,`:199`〜`:201` | **一致**。`:206` は `CheckSuperAdminRole` 付き。`:199-200` は `.Use()` (`:201`) より**前**で無認証 |
| 7 | **`:895` `CountAdminsByContractID` はロック状態を見ない** | `hassan-v2-backend/db/queries/account.sql:80`〜`:81` | **一致**。`WHERE contract_id = $1 AND auth_role_id = 1` のみ。「移植時に `AND last_locked_at IS NULL` を加える」という指摘は正しい |
| 8 | `:714` ⑦の実例 = ロック解除は `WHERE id = $1` | `同:73`〜`:78` (`UnlockAccountByID :execrows`) | **一致** |
| 9 | **`:684`〜`:688` F-15 の機構** (SQL は `WHERE account_id = $1` を持つが呼び出し側が存在確認のみ) | `hassan-v2-backend/db/queries/theme.sql:16`〜`:24` / `usecase/theme/list_themes.go:56`〜`:63` | **一致**。`:56` で `GetAccountByID`、`:60`〜`:62` で `account == nil` のみ判定し `:63` でクエリへ渡す。契約照合は無い |
| 10 | `:376` `GenerateSecureToken` は `math/rand.Read`。用途は①ダミー一時パスワード②管理者パスワード登録メールのトークン (1 週間有効) | `hassan-v2-backend/util/util.go:3`〜`:5`,`:30`〜`:36` / `usecase/admin_account/create_admin_account.go:64`,`:90`,`:95`〜`:100` | **一致**。import は `math/rand` のみ。`:64` = ダミー (`:68` で bcrypt)、`:90` = 登録トークン、`:99`〜`:100` が `time.Hour*24*7` |
| 11 | `:628` v2 の `signup_links` は 5 列で所有者列を持たない | `hassan-v2-backend/db/schema.sql:342`〜`:348` | **一致** (`id`/`email`/`expired_at`/`created_at`/`updated_at`)。**ただし v3 の決定 (DM-A4=B) と食い違う → 重大 4** |
| 12 | `:527` 管理者を対象にした解除クエリは v2 に無い | `hassan-v2-backend/db/queries/account.sql:56`〜`:81` | **一致**。`last_locked_at` を触る 4 クエリはすべて `accounts` 向け |
| 13 | `:1024` v2 はロックだけは区別して返す (`AccountLocked` / `AccountHasBeenLocked`) | `hassan-v2-backend/usecase/account/sign_in.go` (ロック判定 → `AccountLocked`、失敗加算後 → `AccountHasBeenLocked`) | **一致**。加えて**ロック判定がパスワード検証より前**にあることを確認 |
| 14 | `:202` `sharing_settings` は `contract_id` を持つ | `hassan-v2-backend/db/schema.sql:491`〜`:499` | **一致** (`PK (contract_id, category)` + `is_shared boolean NOT NULL`) |

**誤りは 0 件**。DR-1 (出典なしの断定) は §6 の全表について出典が付いており、**照合した 14 件すべてが正確**。
4 巡目の L1 (行番号の誤り) も訂正されている。**全数照合への切り替えは不要**と判断した。

### 1.3 auth.md に**書かれていない** v2 の事実 (本レビューで新規に測定。重大 1 / 重大 6 の根拠)

| # | 事実 | 一次ソース |
|---|---|---|
| N1 | **MFA コード不一致が HTTP 500 になる** — `VerifyTotp` は `err != nil` をすべて `internalServerError` に流す (`apperror.TotpCodeNotMatch()` も同じ経路) | `hassan-v2-backend/controller/mfa.go:80`〜`:83` (`ResetTotp` も `:113`〜`:116` で同型) |
| N2 | **MFA 検証失敗はロックカウンタを増やさない** — `UpdateFailedSignInAttempts` の非テスト呼び出し元は **`usecase/account/sign_in.go:91` の 1 箇所のみ** (`grep -rn "UpdateFailedSignInAttempts" --include="*.go"` の結果は他に interface 宣言・repository 実装・sqlc 生成・テストスタブ 2 件) | `usecase/account/sign_in.go:91` / `usecase/mfa/verify_totp.go:56`〜`:60` (失敗時は activity log のみ) |
| N3 | **`POST /mfa/totp/reset` は現在の TOTP コードを要求する** — デバイス紛失時には使えない (紛失時の経路は `POST /accounts/mfa/reset` = 管理者実行の方) | `usecase/mfa/reset_totp.go:35`〜`:38` |
| N4 | **`sharing_settings` は 3 カテゴリ (`idea` / `asset` / `business_plan`) で 18 の読み取り経路をゲートしている** — テーマ一覧・アセット一覧/取得/最近使用・アイデア取得・ボード一覧/ボード内アイデア・企画書一覧/取得/チャット/詳細系 6 本 | `usecase/theme/list_themes.go:44` / `usecase/asset/{list_assets.go:72,get_asset.go:48,list_recent_used_assets.go:70}` / `usecase/idea/get_idea.go:60` / `usecase/idea_board/{list_idea_boards.go:32,list_ideas_for_board.go:66}` / `usecase/business_plan/…` 6 件 / `entity/sharing_setting.go:38`〜`:47` |
| N5 | `admin_accounts` の**全契約横断ロック解除 (`router.go:211`) は `CheckSuperAdminRole` を持たない** — `.Use()` (`:201`) 配下だが SuperAdmin 判定は付いておらず、**Admin ロールの社内管理者でも実行できる** | `hassan-v2-backend/router/router.go:201`,`:206`,`:211` |

### 1.4 DR-8 (修正の波及漏れ) の全数チェック — grep の生出力

**手順**: 4 巡目の反映で変更 / 新設された主張のキーワードを `docs/` `aidlc-docs/inception/` 全体に grep し、
ヒット全件を auth.md の現行主張と突き合わせた (`aidlc-docs/reviews/` は成果物でないため除外)。

#### G1. 許可リストの種類数 (auth.md `:698`,`:704` = **7 種**)

```
$ grep -rn "2 種のみ\|5 種のみ\|7 種に限る\|許可リスト (7 種)\|許可リストの 7 種" docs/ aidlc-docs/inception/
docs/design/auth.md:698:  | 許可される種類 | **7 種に限る** (下表)。…
docs/design/auth.md:1083:| A-4 … 例外は許可リストの 7 種のみ …
docs/design/API/README.md:158:… 例外は `../auth.md` §6.4 の許可リスト (7 種) に列挙されたものだけ …
docs/design/architecture.md:788:| A-4 絞り込みの層 | 回答 | … 所有者引数の無い単一取得メソッドを CI で禁止 …
  **例外は「未認証経路のクエリ」と「所有者を決定するクエリ」の 2 種のみで、許可リスト + CODEOWNERS 承認で管理する** …
```

→ **波及漏れ 1 件**: `docs/design/architecture.md:788` が **2 種**のまま (7 種への 2 段の拡張 (2→5→7) が
どちらも届いていない)。同行の「**単一取得**メソッドを CI で禁止」も 4 種 (`Get*`/`List*`/`Count*`/`Search*`) への
拡張が未反映。→ **中 1**

#### G2. 認証系統の数 (auth.md `:788`,`:793`〜`:797` = **4 系統**)

```
docs/design/API/README.md:110:… 公開 / ユーザー認証 / 社内管理者認証 / 社内管理者認証 (MFA 未検証で可) の 4 系統 … ✓
docs/design/API/settings.md:199:… 認証系統の分離は同 §6.7 の 4 系統ホワイトリストが担う ✓
aidlc-docs/inception/productionization/requirements.md:49-50:… 4 系統の列挙 + 系統単位の一致 ✓
aidlc-docs/inception/productionization/plan.md:21:… 4 系統ホワイトリスト + 系統単位の CI 検査 ✓
```

→ **波及漏れなし** (4 巡目 §15.4 の自己検出が効いている)。**ただし系統の内容に別の欠陥がある** → **中 3**

#### G3. 除外リストの件数

```
$ grep -rn "除外リスト" docs/ aidlc-docs/inception/
docs/design/auth.md:601:3. **機械検査**: **除外リスト (下記の 9 件)** に無いテーブルが `contract_id` を持たない状態を CI で検出する。
docs/design/auth.md:605:   下表はこの 8 件の根拠であり、**`contract_id` を持つ accounts / companies / signup_links は
docs/design/auth.md:606:   除外リストに入れない** (検査を通る)
docs/design/architecture.md:764:… **例外 (除外リスト) の実体は 9 件で、理由が 2 種類ある** — (a) 所有者列を持たない 7 件 /
  (b) `account_id` は持つが `contract_id` を持たない 2 件 (2026-07-30 更新: signup_links は所有者列を
  持たないことが判明し (a) へ移動) …
docs/design/data-model.md:193:| ① | … **除外リストは §4.1.2 の (a) 表 6 件 + … 2 件 = 8 件に限る** (2026-07-31 の DM-A4=B で
  signup_links が除外から外れた) …
docs/design/data-model.md:394-396:> **検査①の実装は (a) 6 件 + 本注記の 2 件 = 8 件を除外リストに持つ** …
  **accounts / companies / signup_links は除外リストに入れない** (いずれも contract_id を持つ)
docs/design/data-model.md:1061:| 1 | … **除外リストは §4.1.2 (a) の 6 件 + … = 8 件に限る** (… DM-A4=B) …
```

→ **波及漏れ 3 件 + 自己矛盾 1 件**: **同一段落内で 9 件 (`:601`) と 8 件 (`:605`) が並んでいる** /
architecture.md `:764` が **9 件 (7+2)** / auth.md `:628` に `signup_links` の除外行が残存。
**data-model.md `:193` / `:1061` は CI 検査の期待値であり 8 件**。→ **重大 4**

#### G4. 「共有機能を持たない」(A-7)

```
$ grep -rn "共有機能を持たない" docs/ aidlc-docs/inception/
docs/design/architecture.md:791:… よって旧記述「初期増分では共有機能を持たない」は**取り下げる**。共有範囲・失効・ロール移行は
  docs/design/auth.md と docs/design/API/idea-boards.md を SSOT とする |
docs/design/auth.md:1086:| A-7 共有・公開 | **対象外 (暫定)** | … 本増分では共有機能を持たない
  (`architecture.md` §5 と同じ判断)。…
docs/design/API/auth-accounts.md:564:… **auth.md §7 の「本増分では共有機能を持たない」と整合させた** …
docs/design/API/idea-boards.md:310:… **auth.md §7 の「本増分では共有機能を持たない」と食い違う** …
docs/design/API/README.md:466:… **auth.md §7 の「本増分では共有機能を持たない」と食い違うため §5 API-Q3 に残課題として記載** …
```

→ **循環参照 + 判断の空白**: auth.md は「architecture.md §5 と同じ判断」と書き、architecture.md は
「旧記述を**取り下げる**。SSOT は auth.md」と書いている。**取り下げられた判断を根拠にしている**。
さらに `auth-accounts.md:564` が**この陳腐化した記述に自分を合わせている** (伝播が始まっている)。→ **重大 2**

#### G5. R-1 (アカウント基盤の二重化) への依存記述

```
$ grep -rn "R-1 の\|R-1 で決める\|R-1 で確定\|§10.2 R-1" docs/ aidlc-docs/inception/
docs/design/auth.md:633:**この列挙の最終形は §10.2 R-1 で確定する** …
docs/design/auth.md:1082:| A-3 … 例外はアイデンティティ・テナント基盤テーブルの有限列挙のみ (同節。最終形は §10.2 R-1 で確定) |
docs/design/auth.md:1124-1131:**ただし次の 3 項目は R-1 … の結論に依存し、その決定を待って最終形が確定する** …
docs/design/auth.md:1217-1222:… 共有・同期の有無は **§10.2 R-1 で決める** …
docs/design/auth.md:1255:### 10.2 未着手 (Q-A8 の決定から新たに生じた作業)
docs/design/auth.md:1259:| **R-1** | データモデル / 移行設計 (`docs/design/data-model.md` 未着手・AC-3.5 …
docs/design/data-model.md:368:列挙の最終形は auth.md §10.2 R-1 (アカウント基盤の扱い) の決定で確定する。
docs/design/data-model.md:1005:**これで auth.md §10.2 R-1 への回答が確定し、Task-3i の起草ブロックが解ける**
docs/design/data-model.md:1109:| **DM-A3** | アカウント基盤の二重化 (5 点) | §6.5 の [Answer]。推奨を提示済み | ユーザー判断 (auth.md §10.2 R-1) |
aidlc-docs/inception/productionization/plan.md:231:> 併せて auth.md §6.3 の例外テーブル列挙と §6.4 の許可リストも R-1 で確定させる。
```

→ **波及漏れ 6 件**: `data-model.md:1003`〜`:1005` に **DM-A3 = 推奨 5 点すべてで確定 (2026-07-31 ユーザー回答)** の
`[Answer]` があり、`:1021` に **DM-A4 = B 確定**がある。**R-1 は回答済み**であるにもかかわらず、
auth.md 側の 6 箇所 (`:633` / `:1082` / `:1124`〜`:1131` / `:1217`〜`:1222` / `:1259`) が
「未着手」「R-1 で決める」「R-1 の決定を待って確定する」のまま。**data-model.md `:1109` / `:368` も
自書内で stale** (`:1005` で確定と書きながら `:1109` は「ユーザー判断」)。→ **重大 4 / 中 4 / §6 の状態表**

#### G6. 「すべてのテーブル」「すべてのクエリ」

```
docs/design/auth.md:590:**contract_id が全機能テーブルで必須**である一方、account_id は個人境界のものだけが持つ ✓ (DM-2 と一致)
docs/design/auth.md:653:| Repository | 単一取得系も含め、**すべてのクエリが所有者条件を WHERE に持つ** …
docs/design/auth.md:667:… **CI 検査の対象は読み取り系クエリの全種** — Get* / List* / Count* / Search* …
```

→ **自己矛盾 1 件 (4 巡目 M8 の未対応)**: `:653` が「すべてのクエリ」と書き、`:667` の検査は
**読み取り 4 種のみ**。`Update*` / `Delete*` は検査外で、**§6.9 のテナント検証 (UPDATE) と
§5-11 の再発防止が機械強制されない**。→ **重大 5**

#### G7. 「未回答」「すべて確定」

```
docs/design/auth.md:1112:**本書の設計判断 (§6.1〜§6.11) はすべて確定した**。未回答の [Answer] はゼロ。
docs/design/auth.md:1137:**認証設計に関する未回答はゼロ**。派生した確認事項は §10 …
$ grep -c "auth.md" docs/design/API/auth-accounts.md → auth.md 宛の是正要求 R-AA-1 / R-AA-2 / R-AA-8 / R-AA-9 / R-AA-12 = 5 件、いずれも「未対応」
```

→ **「すべて確定」は成立していない**: 2026-07-31 に `auth-accounts.md` §5 が auth.md 宛の是正要求を 5 件
起票し (`:538`,`:539`,`:546`,`:547`,`:549`)、うち **R-AA-1 (MFA 検証のレート制限) は設計の穴**、
**R-AA-12 (AA-Q2=a で確定した「一般アカウントの MFA リセット」) は §6.2 の列挙の欠落**である。
→ **重大 1 / 中 2**

**DR-8 の集計: 波及漏れ 11 件** (G1 = 1 / G3 = 4 / G4 = 1 / G5 = 6 のうち auth.md 側 5 + data-model.md 側 2 を
重複なく数えると 11)。**「5 巡連続で最上位が同じ型」の記録は途切れていない**。
ただし性質は変わっている — 4 巡目までは「自分が直した節の外に旧記述が残る」型だったが、
今回は**外部文書 (data-model.md / auth-accounts.md) が 2026-07-31 に確定させた事実が auth.md に届いていない**
= **受信側の漏れ**である。**§10.2 の R-x 表は「auth.md が他書に出す要求」しか持たず、
「他書から auth.md への要求」を受ける欄が無い**ことが構造的な原因 (→ 中 7 で改善案)。

---

## 2. 重大 (Must Fix)

### 重大 1. `docs/design/auth.md:510`,`:515`〜`:518`,`:1042`〜`:1051` — **MFA を「ロックの代わりの主たる防御」に据えたのに、MFA コードの試行回数に上限が無い**。全契約横断のロック解除 API が 6 桁 = 10^6 の総当たりで抜ける

**該当**:
- `:515`〜`:518` (「**社内管理者にサインイン失敗ロックを設けない**。代わりに **MFA を必須にする**」)
- `:557` (「MFA を主たる防御とし、IP 制限は多層防御として併用する (どちらかで代替しない)」)
- `:1051` (§6.11-3 の対象 = 「サインイン / パスワードリセット要求 / 招待受諾など**未認証で叩けるエンドポイント**」)
- `:797` (§6.7 の 4 系統目 = 「**社内管理者認証 (MFA 未検証で可)**」に MFA 登録・検証が載る)

**事実 (一次ソースで測定。§1.3 の N1・N2)**:

| # | 事実 | 出典 |
|---|---|---|
| 1 | **MFA 検証失敗はロックカウンタを一切増やさない** — `UpdateFailedSignInAttempts` の非テスト呼び出し元は 1 箇所だけで、それは**パスワード検証失敗**の枝である | `hassan-v2-backend/usecase/account/sign_in.go:91` (呼び出し元の全数) / `usecase/mfa/verify_totp.go:56`〜`:60` (失敗時は activity log のみで DB 更新なし) |
| 2 | 社内管理者には**そもそもロック列が無い** (§6.2 の決定でも v3 に作らない) | `hassan-v2-backend/db/schema.sql:52`〜`:64` |
| 3 | **`/mfa/totp/verify` は認証済みエンドポイント**であり、§6.11-3 が定義したレート制限の対象 (「未認証で叩けるエンドポイント」) に**構文上入らない** | `docs/design/auth.md:1051` / `hassan-v2-backend/router/router.go:229`〜`:232` |

**なぜ本番で問題になるか**: §6.2 は「①社内管理者にロックを設けない ②代わりに MFA を必須にする」を
セットで採った。この決定が成立するのは **MFA が有限回で破れないこと**が前提だが、
**v3 の設計上、社内管理者の MFA 検証には (a) 失敗回数のカウンタも (b) レート制限も無い**。
帰結として、**`/admin/signin` でパスワードを通した攻撃者は MFA 未検証トークンを 1 本得たうえで
`POST /admin/mfa/totp/verify` を無制限に叩ける**。TOTP は 6 桁 = 探索空間 10^6、時刻ずれ許容で
同時に有効なコードが複数ある実装が通例のため、**並列で数分〜数十分**の規模である。
突破後に得られるのは §6.2 / §6.9 が定義した**全契約横断のロック解除 (テナント検証を行わない)** と
**SuperAdmin なら他管理者の MFA リセット** — v3 で最も権限の強い経路である。
さらに N1 により **コード不一致が 500 で返る v2 実装を移植すると、総当たりは「5xx の山」として現れ**、
O-4 / O-7 のアラートは意味を失う (`observability.md:197` が「握り潰すと総当たり・パスワードスプレーの
検知手段が無くなる」と書いた事象そのものが、MFA については設計に入っていない)。

**独立の裏付け**: `docs/design/API/auth-accounts.md:307` (AA-D-10) と `:538` (R-AA-1) が
**同じ穴を 2026-07-31 に独立して発見し、auth.md §6.11-3 への是正要求として起票している** (状態 = **未対応**)。
本レビューは一次ソース (N1 / N2) から同じ結論に到達した。**2 経路から同一の穴が指摘されている**。

**修正案** (auth.md 側で閉じるべき最小の書き足し):

1. **§6.11-3 の「対象」行を書き換える** — 現行「未認証で叩けるエンドポイント」を
   「**①未認証で叩けるエンドポイント ②資格情報・ワンタイムコードを検証する認証済みエンドポイント
   (`POST /mfa/totp/verify` / `POST /admin/mfa/totp/verify` / パスワード再入力を伴う操作)**」に拡張する。
   キーは②では `account_id` / `admin_account_id` + エンドポイント (IP ではない — 認証済みなので主体が特定できる)。
2. **§6.2 に「MFA を主たる防御にすることの成立条件」を 1 行で明記する** —
   「**MFA 検証の試行回数に上限を設けること (§6.11-3 の②) が、ロックを設けない判断の成立条件である**」。
   条件として書かないと、実装リポで②が落ちたときに「ロックも無い・上限も無い」状態が
   **設計どおりとして**成立する。
3. **§6.6 の表に MFA 検証失敗の行を追加する** — 「**MFA コードが不正 = 401 + `CodedError`**」
   (v2 は 500。N1)。併せて **「MFA コードの試行超過 = 429」**。**§6.6 には現在 MFA 検証失敗の行が無く**、
   `admin` 側の MFA を新設したにもかかわらずステータスコードの SSOT が沈黙している。
4. **上限超過時の挙動を決める** — 一般ユーザー側は `failed_sign_in_attempts` と別カウンタにするか
   同一にするかを書く (`§10.2 R-4` の「ロックしきい値 < レート制限しきい値」の関係に第 3 のしきい値が入るため、
   3 者の関係を R-4 の制約に含める)。

---

### 重大 2. `docs/design/auth.md:1086` (§7 A-7) — **A-7 の判断を持つ文書が 1 つも無くなった**。auth.md は「architecture.md と同じ判断」と書き、architecture.md はその判断を**取り下げている**

**該当**: `docs/design/auth.md:1086` / `docs/design/architecture.md:791` / `docs/design/API/README.md:466`,`:484` /
`docs/design/API/idea-boards.md:310` / `docs/design/API/auth-accounts.md:564` / `docs/design/data-model.md:820` /
`docs/design/API/themes.md:153` / `docs/design/API/assets.md:164` / `docs/design/frontend.md:979`

**矛盾の構造** (G4 の grep が示すとおり):

| 文書 | A-7 の状態 |
|---|---|
| `auth.md:1086` (**自ら SSOT を宣言**: `:13`) | **対象外 (暫定)**。根拠は「`architecture.md` §5 と同じ判断」 |
| `architecture.md:791` | 「**共有は既に稼働している**。旧記述『初期増分では共有機能を持たない』は**取り下げる**。SSOT は `auth.md` と `API/idea-boards.md`」 |
| `data-model.md:820` (DM-9) | **回答 (スキーマ側)**。`visibility` 列を増分 1 でスキーマに用意、書き込みは増分 2、既存 `sharing_settings` は移行時の初期値 |
| `API/themes.md:153` / `API/assets.md:164` | **部分 / 回答**。TM-1 / AS-M1 に**移行規則が確定済み** |
| `API/idea-boards.md:310` | **回答**。3 段ロールを引き継ぐ。「auth.md §7 と食い違う」 |
| `API/README.md:466` | **部分回答**。「auth.md §7 と食い違う」→ API-Q3 |
| `API/auth-accounts.md:564` | **回答**。「**auth.md §7 の『持たない』と整合させた**」= **陳腐化した記述に自分を合わせた** |
| `operations.md:458`,`:822` / `API/settings.md:219` | `sharing_settings` の移行**実行タイミングまで確定済み** |

つまり **auth.md → architecture.md → auth.md の循環参照**であり、その循環の起点は
**architecture.md が明示的に取り下げた文**である。加えて下流 6 文書が既に A-7 の具体設計
(`visibility` 2 値・列は増分 1・書き込みは増分 2・`sharing_settings` からの初期値・ボードは 3 段ロール引き継ぎ・
`open` は採らない) を持っている。**判断は事実上できているのに、SSOT がそれを宣言していない**。

**なぜ本番で問題になるか (「対象外」が事実として成立しないことの実測)**: v2 の `sharing_settings` は
アイデアボード限定の機能ではなく、**契約 × 3 カテゴリ (`idea` / `asset` / `business_plan`) の
読み取りスコープを決めるゲートであり、18 の読み取り経路が参照している** (§1.3 の N4。
`usecase/theme/list_themes.go:44` / `usecase/asset/list_assets.go:72` / `usecase/idea/get_idea.go:60` ほか)。
`entity/sharing_setting.go:38`〜`:47` が値域を検証している稼働機能である。
したがって v3 が A-7 を「持たない」で出すと、切替時に**必ず**次のどちらかが起きる:

- `is_shared = true` の契約 → **同僚のテーマ・アセット・アイデア・企画書が見えなくなる** (機能退行。
  ユーザーには「データが消えた」と見える)
- 既定を契約可視にする → `is_shared = false` の契約 (**v2 の既定は非共有** — `API/README.md:79` F-16) で
  **テナント内の他人のアセット本文が一斉公開になる** (`asset_documents` 相当の機微データ)

これは `review-auth-api.md` の重大 7 が指摘した「既に稼働している共有機能に対して『持たない』は
選択肢として存在しない」の**定量的な裏付け**であり、影響範囲はボード 1 機能ではなく **4 ドメイン**である。

**修正案** (決着案は §5 の重大 7 に詳述。auth.md 側の最小作業):

1. **§6 に §6.12 (A-7 共有・公開) を新設し、単一の判断を書く** — (a) `sharing_settings` は**引き継がない**が
   **既存値は移行時の `visibility` 初期値に使う** (SSOT は `data-model.md` DM-9 / `API/themes.md` TM-1 /
   `API/assets.md` AS-M1) (b) **アイデアボードは v2 の契約内共有と 3 段ロールを増分 1 でそのまま引き継ぐ**
   (SSOT は `API/idea-boards.md` D-IB-8 / §3.1 / §4 M-4) (c) **テーマ・アセットの per-resource `visibility`
   書き込み API は増分 2** (D-API-8') (d) **契約外公開 (`open`) は本増分でも後続増分でも持たない**
   (`API/themes.md` D-TH-3 の却下と揃える)。
   却下案として「**A-7 全体を対象外にする案** — v2 の `sharing_settings` が 18 の読み取り経路をゲートしており
   (N4)、切替が機能退行か一斉公開のどちらかになるため成立しない」を残す。
2. **§7 の A-7 行を「回答 (§6.12)」に更新し、`architecture.md:791` への「同じ判断」参照を削除する**。
3. **`API/README.md:484` の API-Q3 を「テーマメンバーの権限差 (TH-Q5) の要件確認のみ」に縮小**する
   (A-7 の判断そのものは残課題から外す)。
4. **`API/auth-accounts.md:564` の「auth.md §7 と整合させた」という根拠付けが無効になる**ことを
   同書の担当セッションへ申し送る (**別レビュアーの担当範囲と重複する可能性あり**。
   同書の判断内容 = 「`GET /contracts/me` から `sharing_settings` を落とす」自体は §6.12 の (a) と整合するため、
   直すのは根拠の書き方のみ)。

---

### 重大 3. `docs/design/auth.md:652`,`:657`,`:777`〜`:779` — **`review-auth-api.md` の重大 3 が 2 巡連続で未対応**。「read-then-compare は却下」「403/404 の取り違えが構造的に起きない」がリソース単位ロール (403 が 8 本) と両立しない

**該当**: `:652` (UseCase 行「取得後の read-then-compare を**主たる防御にしない**」) /
`:657` (「**却下 (a) UseCase の read-then-compare を正とする (v2 パターン B)**」) /
`:777`〜`:779` (「他テナントのリソースは『0 件』として返る … **403 と 404 の取り違えが
構造的に起きない**」) / 対する API 側: `docs/design/API/README.md:185`〜`:193`,`:346`,`:461` (R-2 = 8 本) /
`docs/design/API/idea-boards.md:182` (D-IB-11),`:192` (§3.1)

**未対応であることの確認**: `review-auth-api.md:184`〜`:221` の修正案 3 点 (①§6.4 に第 3 のパターンを追加
②§6.6 の断定を限定 ③網羅 UT を D-2 のマージ条件に入れる) は **1 つも反映されていない**
(`grep -rn "リソース単位ロール" docs/design/auth.md` → **0 件**)。
`review-auth-hardening.md` §14 / §15 でも扱われていない (§14 は B1〜B3 のみを対象にした)。

**両立しない理由 (一次ソースで再確認済み)**: `API/README.md:185`〜`:193` の R-2 は
**viewer に 403 / 非メンバーに 404** を要求する。この 2 つを区別するには
「ボード行を `contract_id` で取得したうえで `viewer_account_ids` / `editor_account_ids` を評価する」必要があり、
**メンバーシップを `WHERE` に入れてはいけない** (入れると viewer も 0 件 → 404 に落ちる)。
v2 も実際にこの形である — `usecase/idea_board/list_idea_boards.go:32` で `sharing_settings` を読み、
`:28` の `ListIdeaBoardsByContractID` (`WHERE contract_id = $1`) で取得したのち
`:44` の `if b.HasAccess(input.AccountID)` で絞る (`idea-boards.md:182`,`:322` が同じ照合を報告しており一致)。
**これは §6.4 が却下 (a) として明示的に却下したパターン B そのもの**である。

**なぜ本番で問題になるか**: auth.md は `:13` で「v3 の認証・認可・テナント設計の判断 (§6 / §7) は
**このファイルが SSOT**」と宣言している。**SSOT が「read-then-compare は却下」「WHERE で絞れば
取り違えは起きない」と書いているため、ボード・アイテム・コメントを実装する開発者は
メンバーシップを `WHERE` に入れる方向へ誘導される**。そうすると viewer が 404 を受け取り、
`API/README.md` §2.5 の 403 設計 (8 本) が**静かに壊れる** (「共有されたのに見えない」= ユーザーには
共有機能の不具合として現れ、認可の設計ミスとしては気付かれない)。
逆に §3.1 に従えばロール判定が Go 側の分岐に戻り、**§5-1 (`GET /themes/:id` の IDOR) を生んだのと
同じ「実装者が書き忘れると素通り」構造**が、auth.md の中では「構造的に起きない」と書かれたまま残る。

**新たな観測 (決着を容易にする材料)**: **§6.4 は既に read-then-compare を 1 つ制度化している** —
許可リストの**種別⑥「一意キーで引いた直後に契約検証を行うクエリ」** (`:713`) は、
「取得してから Go 側で契約を比較する」形を**登録条件 (契約検証の実装箇所を必須項目にする) 付きで許可**している。
一次ソースでもこの形が正しいパターンとして機能している (`usecase/account/reset_member_mfa.go:51`〜`:62` =
ID で取得 → `target.ContractID != input.AuthAccount.ContractID` で `AccountNotFound`)。
**したがって「パターン B を全面却下」ではなく「登録条件付きで許可する第 3 のパターン」という
既存の枠がそのまま使える**。決着案は §5 に示す。

**修正案**: §5 の「重大 3 の決着材料」を参照 (採用案 / 却下案 / 理由の形で提示した)。

---

### 重大 4. `docs/design/auth.md:601` vs `:605` vs `:628` — **除外リストの件数が同一段落で 9 件と 8 件に分裂し、`signup_links` の扱いが 2026-07-31 の DM-A4=B と逆**。**CI 検査の期待値が 3 文書でずれている** (DR-9)

**該当**:

| 箇所 | 記述 | 判定 |
|---|---|---|
| `docs/design/auth.md:601` | 「**除外リスト (下記の 9 件)** に無いテーブルが `contract_id` を持たない状態を CI で検出する」 | **誤り (8 件が正)** |
| `docs/design/auth.md:603` | 「(a) 所有者列をまったく持たない **7 件** / (b) `account_id` は持つが `contract_id` を持たない **2 件**」 | **誤り** ((a) は 6 件が正) |
| `docs/design/auth.md:605` | 「下表はこの **8 件**の根拠であり、**`contract_id` を持つ `accounts` / `companies` / `signup_links` は除外リストに入れない**」 | **正しい** (同一段落内で `:601` と矛盾) |
| `docs/design/auth.md:628` | `signup_links` の除外行「**所有者列を 1 つも持たない** … **未認証経路からリンク ID で引く**ため所有者条件を掛けられない」 | **誤り** (DM-A4=B により `contract_id NOT NULL` + FK を持つ) |
| `docs/design/architecture.md:764` | 「例外 (除外リスト) の実体は **9 件** … (a) 所有者列を持たない **7 件** / (b) … **2 件** (2026-07-30 更新: `signup_links` は所有者列を持たないことが判明し (a) へ移動)」 | **誤り (2 段前の版)** |
| `docs/design/data-model.md:193` (**§3.3 の CI 検査①**) | 「除外リストは §4.1.2 の (a) 表 **6 件** + (b) 表のうち 2 件 = **8 件**に限る (2026-07-31 の DM-A4=B で `signup_links` が除外から外れた)」 | **正 (SSOT)** |
| `docs/design/data-model.md:1061` (**§7.2 の検査 1**) | 同上 **8 件** | **正 (SSOT)** |

**一次ソースでの確認**: v2 の `signup_links` が 5 列で所有者列を持たないことは事実
(`hassan-v2-backend/db/schema.sql:342`〜`:348` を確認・一致)。**auth.md `:628` は「v2 の事実」としては正しいが、
「v3 の除外リストに入れる」という結論が 2026-07-31 の決定で反転した**。
根拠: `docs/design/data-model.md:1021`〜`:1024` の `[Answer]` = 「**B — `contract_id NOT NULL` + FK を持たせる**
(2026-07-31 ユーザー回答)。… **§4.1.2 の除外リストは 8 件に戻す**」+ `:389` (§4.1.2 の `signup_links` 行 =
「**通る**」) + `:410` (§4.2 の列追加)。

**なぜ本番で問題になるか (DR-9 の典型例)**: **この件数は CI 検査の期待値**である。
`data-model.md:193` (検査①) と `:1061` (§7.2 の検査 1) は「除外リストは 8 件に限る」と書き、
**auth.md §6.3-3 は同じ検査を「9 件」と定義している**。auth.md は
`aidlc-docs/inception/productionization/requirements.md:59` (AC-1.2) が**名指しで SSOT にしている文書**であり、
**実装リポは auth.md を読んで検査を書く**。結果として:

- **9 件で実装した場合**: `signup_links` が除外されるため、**将来 `signup_links` から `contract_id` が
  落ちても検査が検出しない** (data-model.md `:193` が「除外すると将来 `contract_id` が落ちても
  検出できない」と明記して避けた事態がそのまま入る)
- **8 件で実装した場合**: auth.md の例外表 (`:628`) に `signup_links` が「所有者列を持たない」として
  載っているため、**設計書とスキーマが矛盾した状態で実装が進む** (テーブル定義に `contract_id` を
  入れるべきか判断できない)

さらに **`make check-table-counts` はこの件数を検算していない** — `scripts/check-table-counts.sh:107`〜`:113` が
auth.md について照合するのは機能テーブル数 (40) / 個人境界 (32) / 契約境界 (8) の 3 値のみ。
**`.claude/rules/feedback_review_patterns.md` DR-9 の運用注記「新しく『N 件』を書くときは、同時に検算の
対象に加えるか、書かずに定義元へのリンクにする」に対する未達**である。

**修正案**:

1. **auth.md `:601` の「9 件」を削除し、件数を書かずに定義元へのリンクにする** —
   「除外リストの実体は `docs/design/data-model.md` §4.1.2 の 2 表が SSOT
   ((a) 所有者列を持たない表 + (b) のうち `contract_id` を持たない 2 件)」。
   **これが DR-9 の推奨形 (件数を転記しない)** であり、次の増減で再発しない。
   `:603` の「7 件 / 2 件」も同様に (a)(b) の**理由の区別のみ**を残して件数を落とす。
2. **auth.md `:628` の `signup_links` 行を例外表から削除する** — 代わりに `:625` の
   「認証フローの一時レコード」行から `signup_links` を外し、**§6.4 の許可リストへ移す**
   (`auth-accounts.md:433` が既に `GetSignupLinkByToken` を**種別①**として登録済みなので、
   §6.4 の①の実例にリンク ID 引きを 1 行追加すれば整合する)。
3. **`architecture.md:764` の「9 件 / 7 件 / 2 件」も同様に件数を落とし、`data-model.md` §4.1.2 への参照にする**。
4. **`scripts/check-table-counts.sh` に除外リストの検算を追加する** (定義元 = `data-model.md` §4.1.2 の 2 表の
   行数を実測し、転記先を照合する)。**または上記 1 / 3 で転記を全廃する** — DR-9 の運用は
   「検算に入れる **か** 書かない」なので、どちらか一方でよい。**書かない方を推奨**する
   (この件数は 2026-07-30 に 9 件へ、2026-07-31 に 8 件へと 2 日で 2 回動いており、
   増減の頻度が高い集合である)。
5. **併せて `:633` / `:1082` の「この列挙の最終形は §10.2 R-1 で確定する」を削除する** —
   R-1 は **2026-07-31 に回答済み** (`data-model.md:1003`〜`:1005` の DM-A3 = 推奨 5 点すべてで確定)。
   **列挙は今確定させられる**。宙吊りのままにすると、実装リポが「まだ決まっていない例外表」を
   前提に検査の実装を止める。

---

### 重大 5. `docs/design/auth.md:653` vs `:667`〜`:672` — 「**すべてのクエリ**が所有者条件を持つ」と書きながら CI 検査は**読み取り 4 種のみ**。**v2 で実際に越境した §5-11 は UPDATE** であり、§6.9 のテナント検証も UPDATE に依存する

**該当**: `:653` (Repository 行「単一取得系も含め、**すべてのクエリが所有者条件を `WHERE` に持つ**」) /
`:667`〜`:672` (「**CI 検査の対象は読み取り系クエリの全種** — `Get*` / `List*` / `Count*` / `Search*`」) /
`:891` (§6.9 のテナント検証 = 「**Repository のクエリ条件** (`WHERE id = $1 AND contract_id = $2`) で強制」) /
`docs/design/API/README.md:158` (同じ 4 種を CI 検査として登録)

**4 巡目 M8 として指摘済み・未対応**であることを確認した (`review-auth-hardening.md:1926` の M8。
`review-auth-hardening.md` §15 の対応記録に M8 は無い)。**格上げの理由は対象の実体化**である:

| 根拠 | 内容 |
|---|---|
| 1 | **v2 で実測された越境の実例 §5-11 は UPDATE** — `DeleteLastLockedAt` = `UPDATE accounts … WHERE email = $1` (`hassan-v2-backend/db/queries/account.sql:66`〜`:71`。照合済み)。**読み取り 4 種の検査ではこの型を 1 件も検出できない** (§6.10-3 が `math/rand` 検査の範囲を「認証パッケージのみにしない」理由として挙げたのと**同じ論法**が、ここでは適用されていない) |
| 2 | **§6.9 の防御そのものが UPDATE の `WHERE` に依存する** — 手動ロック / 解除の「テナント検証 (必須)」は `WHERE id = $1 AND contract_id = $2` であり、**この条件が落ちたことを検出する機構が無い**。§6.9 は §5-11 の再発防止として設計されたのに、その担保が「実装者が書く」に留まる (DR-5) |
| 3 | **対象が 2026-07-31 に実体化した** — `docs/design/API/auth-accounts.md` が 37 エンドポイントを確定させ、うち手動ロック / 解除 / MFA リセット / メンバー更新・削除・権限変更はすべて UPDATE / DELETE である。同書 `:458`〜`:459` は「契約内管理者のロック / 解除 / MFA リセット / メンバー取得・更新・削除は `WHERE id = $1 AND contract_id = $2` を持つので**検査を通る**」と書いているが、**`Update*` / `Delete*` は検査の対象外なので「検査を通る」という表現自体が成立しない** (検査されていない) |

**なぜ本番で問題になるか**: §6.4 冒頭が自ら「**機械強制** (これが無いと上記は『気をつける』に留まる — DR-5)」と
書いている。その基準を読み取り系だけに適用し、**実際に越境が起きた側 (書き込み系) を外している**。
書き込み系の越境は読み取りより被害が重い — 他契約のアカウントのロック解除 (§5-11 の実例) /
MFA の削除 / メンバーの削除はいずれも**他テナントの状態を変える**。

**修正案**:

1. **§6.4 の CI 検査の対象を「読み取り系 4 種 + 書き込み系」に拡張する** —
   `-- name: Update*` / `Delete*` / (必要なら `Insert*` の `WHERE` を伴うもの) を含め、
   **「所有者条件を `WHERE` に持たない書き込みクエリ」を検出する**。
   例外は同じ許可リストで受ける (**種別⑦ (全契約横断の運用操作) が社内管理者の `UnlockAccountByID` を
   既に受けられる形になっている**ので、新しい種別は要らない)。
2. `:653` の「すべてのクエリ」と `:667` の検査範囲の**文言を一致させる** (現状は前者が広く後者が狭い)。
3. **`docs/design/API/README.md:158` の検査定義も同時に更新する** (同じ検査の二重記載であり、
   片方だけ直すと DR-8 になる)。
4. **`INSERT` の扱いを 1 行で決める** — `INSERT … SELECT` や親テーブルの所有者を継承する挿入で
   越境が起きうるが、`WHERE` を持たない `INSERT` を検査対象にすると全件が引っかかる。
   「**`INSERT` は所有者列を必須列として持つこと (§6.3) で担保し、SQL 検査の対象外とする**」と
   明記すれば「無言の省略」にならない。

---

### 重大 6. `docs/design/auth.md:512` — **MFA 登録前の窓がパスワードのみで守られ、その初期パスワードは §5-8 が「より深刻」と認定した v2 の `math/rand` トークン経由で設定されたもの**。攻撃者が自分の TOTP を登録して正規の社内管理者を締め出せる

**該当**: `:512` (「**v3 では API を作らず、移行スクリプトによる投入とする**」「**投入直後は MFA 未登録**なので、
**初回サインイン後に TOTP 登録を強制する** (登録するまで解除 API に到達できない)」) /
`:797` (§6.7 の 4 系統目 = 「社内管理者認証 (MFA 未検証で可)」に **MFA の登録・検証**が載る) /
`:515` (社内管理者にロックを設けない) / `:1262` (R-7 = operations.md への申し送り)

**穴の機構** (すべて設計書と一次ソースから導出):

1. §6.2 は社内管理者に**ロックを設けない**。§6.11-3 のレート制限は `/admin/signin` を対象に含むが
   (`:509`)、**IP + エンドポイント**をキーとするため分散した試行には効きが薄い。
2. `admin_accounts` は移行スクリプトで投入され、**`crypted_password` も同時に入る**。
   その値の出所は書かれていないが、**v2 から移送するなら v2 の管理者パスワード**である。
3. **v2 の管理者パスワードは `math/rand` ベースの登録トークン経由で設定された** —
   `usecase/admin_account/create_admin_account.go:90` が `util.GenerateSecureToken()` (=
   `util/util.go:30`〜`:36`。import は `math/rand` のみ — `同:3`〜`:5`) で 32 バイトのトークンを作り、
   **有効期限 1 週間**でメール送信する (`同:95`〜`:100`)。**auth.md §5-8 自身が「②の方が深刻」と
   認定している経路**である (`:376`)。**照合済み・一致**。
4. **§6.7 の 4 系統目は MFA 未検証トークンで `POST /admin/mfa/totp/generate` に到達できる**。
   v2 の実装 (移植元) は `IsVerified == false` の設定を**削除して再生成を許す**
   (`usecase/mfa/create_totp.go:41`〜`:46`)。**したがって未登録の社内管理者アカウントに対しては、
   パスワードを知る者が自分の authenticator を登録できる**。
5. 登録後は `IsVerified == true` になるため、**正規の管理者は `TotpMfaAlreadyRegistered` で登録できず**
   (`同:36`〜`:38`)、回復には**他の SuperAdmin による MFA リセット**が必要 (`:541`〜`:550`)。
   **SuperAdmin が 1 名なら製品内で回復不能** (auth.md 自身が `:548` で認めているケース)。

**なぜ本番で問題になるか**: §6.2 の決定は「ロックを捨てて MFA を主たる防御にする」だが、
**MFA が有効になるまでの窓では防御が『パスワード 1 要素 + 弱いレート制限』しかない**。
その窓の長さは「移行スクリプト実行 → 各管理者の初回サインイン完了」までで、
**運用手順 (R-7) 側に時間制約が書かれていない**ため無期限になりうる。突破の報酬は
**全契約横断のロック解除**であり、N5 のとおり v2 ではこの操作に SuperAdmin 判定が無い
(`hassan-v2-backend/router/router.go:211` は `.Use()` 配下だが `CheckSuperAdminRole` を持たない) ため、
**Admin ロールでも足りる**。さらに 4 により**正規管理者の締め出し (DoS)** も同時に成立する。

**auth.md 側にこの論点が無いことの確認**: `grep -rn "初期パスワード\|初回サインイン" docs/design/auth.md` →
`:512` の 1 行のみ。R-7 (`:1262`) の項目は「①初期投入 (移行スクリプト) と初回 MFA 登録の手順
②SuperAdmin 2 名以上 ③1 名時の回復手順」で、**パスワードの出所・配布方法・窓の閉じ方が入っていない**
(4 巡目 M6 が「パスワードの初期設定方法 (B1 の⑤と連動)・実行主体・承認が未決のまま消える」と
指摘した部分が R-7 に反映されていない)。

**修正案** (§6.2 に 1 表を足す規模):

1. **v2 の管理者パスワードを v3 へ移送しない**と決める — §6.8-2 が JWT 鍵について採ったのと**同じ論法**
   (「露出済みの値を安全な保管先に入れても意味がない」)。代わりに
   **移行スクリプトは投入時にランダムな一時パスワードを `crypto/rand` で生成し (§6.10)、
   帯域外 (口頭 / 既存の社内認証済みチャネル) で配布する**。
2. **登録窓に期限を持たせる** — 「**投入から N 時間以内に初回サインイン + TOTP 登録を完了しない
   アカウントは無効化する**」(一時パスワードの有効期限として表現する)。期限切れの再発行は
   移行スクリプト / 運用手順の対象。
3. **未登録アカウントの `/admin/signin` を §6.11-3 の②の対象に含める** (重大 1 の修正と同時に閉じる)。
4. **初回登録の完了を監査記録に残す** (O-6。誰がいつ authenticator を登録したか。
   締め出しが起きたときに「いつ誰が登録したか」が分からないと調査できない)。
5. **N5 を §6.2 / §6.9 に事実として書き、v3 での判断を明示する** —
   「v2 の全契約横断ロック解除は **Admin ロールでも実行できる** (`router.go:211` に
   `CheckSuperAdminRole` が無い)。v3 は (a) 同じにする / (b) SuperAdmin 限定にする」。
   **現在の auth.md は §6.9 の社内管理者経路の実行者を「`admin_accounts` (`X-Admin-Token`)」としか
   書いておらず、MFA リセットだけが SuperAdmin 限定 (`:543`) という非対称が理由なしに残っている**。
   権限の強さは「全契約のロック解除」≧「他管理者の MFA リセット」なので、この非対称は説明が必要。

---

## 3. 中 (Should Fix)

### 中 1. `docs/design/architecture.md:788` (§5 A-4 行) — 許可リストが **2 種**のまま。2 段の拡張 (2→5→7) がどちらも届いていない

**該当**: 「**例外は「未認証経路のクエリ」と「所有者を決定するクエリ」の 2 種のみで、
許可リスト + CODEOWNERS 承認で管理する**」「所有者引数の無い**単一取得**メソッドを CI で禁止」。

**現行の auth.md §6.4**: 許可リストは **7 種** (`:698`)、CI 検査は **4 種** (`Get*`/`List*`/`Count*`/`Search*`。`:667`)、
所有者 ID の生成経路は **2 本** (`:673`〜`:690`)。

**なぜ問題か**: `architecture.md` §5 は**本番観点 (A/O/D) の回答表**であり、レビュアーと実装者が最初に読む。
そこで「例外は 2 種のみ」を読んだ実装者は、**種別③〜⑦ (bool 分離・マスタ参照・頂点テーブル・
契約検証付き取得・全契約横断) を「許可されない例外」と判断して停止するか、勝手に分類する**。
4 巡目 B2 が「実装者は誤分類か停止の二択になる」として Freeze ブロッカーにしたのと同じ構造が、
**architecture.md 側に残っている**。→ **§6.4 の現行 3 点 (許可リスト 7 種 / 検査 4 種 / 生成経路 2 本) への
参照に置き換える** (件数を転記せず「詳細は auth.md §6.4」とするのが DR-9 的にも望ましい)。

### 中 2. `docs/design/auth.md:504`〜`:512` (§6.2 の「含める範囲」表) — **2026-07-31 に確定した「一般アカウントの MFA リセット」が列挙に無い**。§6.2 は「有限の列挙」と宣言しているため、実装対象から落ちる

**該当**: §6.2 の 5 行の表 (ロック解除 / 社内管理者のサインイン / MFA 登録・検証 / MFA リセット / 初期投入)。
`:514` は「それ以外の管理者機能は**引き続き対象外**」と閉じている。

**事実**: `docs/design/API/auth-accounts.md:596`〜`:600` の **AA-Q2 = (a) 本増分に含める (2026-07-31 ユーザー回答)**
により、`POST /admin/accounts/{account_id}/mfa/reset` (**社内管理者による一般アカウントの MFA リセット**) が
確定した。v2 に実装がある (`hassan-v2-backend/router/router.go:217`)。同書 `:549` が **R-AA-12** として
auth.md §6.2 への追加を起票済み (状態 = **未対応**)。

**なぜ問題か**: この経路が無いと、**MFA 必須の契約で契約内管理者が全員 MFA デバイスを失った場合に
製品内の回復手段が存在しない** — §6.9 がロックについて解いた「回復経路が無い」と**同型の穴**である
(BE-10)。§6.2 の表が「含める範囲」の有限列挙として機能しているため、**表に無い = 実装しない**と読まれる。
→ 表に 1 行追加し、§6.9 の回復経路の表 (`:918`〜`:925`) にも「**MFA デバイス紛失による契約全体の
ロックアウト**」を経路として追記する (現在の 4 経路はロックのみを扱っており、MFA 起因の到達不能は
経路 4 の管理者側だけが書かれている)。

### 中 3. `docs/design/auth.md:793`〜`:797` (§6.7 の 4 系統表) — **「ロック解除 API」「MFA リセット」がユーザー系統と管理者系統の両方に実体を持つのに、表は片方にしか書いていない**。CI 検査の入力がこの宣言なので誤登録を誘発する

**該当**: `:796` (社内管理者認証の行 = 「**ロック解除 API / MFA リセット (§6.2)**」) /
`:795` (ユーザー認証の行 = 「上記と管理者系を除く**全ルート**」)。

**事実**: §6.9 の「実行者の 2 経路」表 (`:901`〜`:904`) は、**ロック / 解除に契約内管理者経路と
社内管理者経路の 2 本がある**と定めている。前者は**ユーザー認証系統**である。
MFA リセットも同様に 2 本ある — 契約内管理者による**メンバーの** MFA リセット
(§9.3 Q-A2 の 9 操作の 1 つ。`:1184`。v2 は `router.go:83`) と、社内管理者による SuperAdmin 限定のリセット
(`:511`)。**表の文言「ロック解除 API」「MFA リセット」はどちらの実体を指すか読み取れない**。

**なぜ問題か**: §6.7 の宣言は **CI が系統単位の一致を検査する入力**である (`:790`)。
実装者が契約内管理者のロック解除を社内管理者系統に登録すれば**契約内管理者が到達できなくなり**
(通常運用の手段が消える)、逆に社内管理者の解除をユーザー認証系統に登録すれば
**§6.7 自身が `:801`〜`:802` で警告した「契約ユーザーが他テナントのアカウントを解除できる」が成立する**。
→ 表の該当セルを「**社内管理者によるロック解除 (全契約横断) / 社内管理者による MFA リセット
(SuperAdmin 限定) / 社内管理者による一般アカウントの MFA リセット (中 2)**」と**主体付きで書き**、
ユーザー認証行に「**契約内管理者によるロック / 解除・メンバーの MFA リセットはこちら**」を明記する。
併せて `docs/design/API/auth-accounts.md:551` (**R-AA-9**) が指摘する
「ユーザー側の『MFA 未検証で到達可』が系統表に現れていない」非対称も同じ表で閉じられる
(**別レビュアーの担当範囲と重複する可能性あり**)。

### 中 4. `docs/design/auth.md:1124`〜`:1131` / `:1259` (§9.1 の依存表・§10.2 R-1) — **R-1 は 2026-07-31 に回答済み**なのに「未着手」「決定を待って確定する」のまま。3 項目が理由なく宙吊りに見える

**事実**: `docs/design/data-model.md:1003`〜`:1005` の `[Answer]` = 「**推奨 5 点すべてで確定
(2026-07-31 ユーザー回答)**。①v3 を正とする ②RL-3 の最初に移行 ③移行中は v2 のアカウント更新系を
数分停止 ④**資格情報は 1 回コピーのみで同期しない** ⑤切り戻しは v2 を使う (告知付き)」。
これは §10.2 R-1 の「決めること ①〜⑤」に**1:1 で対応している**。

**帰結として直せる箇所** (状態記述だけでなく**設計判断の内容**が変わるものを挙げる):

| 箇所 | 現行 | R-1 回答後の正 |
|---|---|---|
| `:1217`〜`:1222` (Q-A3 の残存リスク) | 「§5-8 の影響範囲は **R-1 の決定次第**」「共有・同期の有無は R-1 で決める」 | **④により「同期しない」で確定** → **v2 側の侵害は v3 に及ばない** (1 回コピーのみ)。ただし**コピーの中身 (パスワードハッシュ) は v2 由来**なので、**重大 6 の論点 (v2 で設定されたパスワードの品質) はここで閉じない**ことを明記すべき |
| `:633` / `:1082` (例外列挙の最終形) | 「§10.2 R-1 で確定する」 | **①v3 を正とする + `data-model.md` §4.2 (v2 と同じ構造で v3 に持つ) により確定可能**。重大 4 の修正と同時に閉じる |
| `:1130` (許可リストの中身) | 「R-1 に伴い認証系クエリの実体が変わる」 | `auth-accounts.md:428`〜`:462` が **13 行の割り当てを確定済み**。auth.md §6.4 は「実体の SSOT は同書 §3.5」と参照すればよい |
| `:1259` (R-1 の行) | 「**未着手**」 | 「**回答済み (2026-07-31)。SSOT は `data-model.md` §6.5**」+ **残る作業は「本書側への転記」**に書き換える |

**注**: 「`docs/design/` に『未着手』と書かれた stale 記述 12 箇所」は**既知・別タスク**と指示されているため、
状態列の書き換えそのものは再指摘しない。ここで挙げたのは**stale によって設計判断が宙吊りになっている
4 箇所**である。

### 中 5. `docs/design/auth.md:763`〜`:772` (§6.6 の表) — **fail-closed の 503 が無い** (4 巡目 M3 の未対応) / **MFA 未検証の判定が「`/mfa` 配下のみ許可」= §6.7 が却下したパス前方一致で書かれている**

**該当**: `:767` (「MFA 必須かつ未検証 | **401** | … v2 と同じ。**`/mfa` 配下のみ許可**」) /
`:772` (429 の行はある) / §6.11-3 の `:1050` (「レート制限の判定ができない場合は … **429 ではなく 503**」)。

**2 点**:

1. **503 が判定規則の SSOT に無い** — §6.11-3 で新設したのに §6.6 の表に行が無い
   (4 巡目 M3 として指摘済み・未対応)。§6.6 は「**下表を全エンドポイントの規約とする**」と宣言しているため、
   表に無いコードは実装されない可能性がある。
2. **`/mfa` 配下のみ許可という書き方が §6.7 と矛盾する** — §6.7 (`:807`〜`:812`) は
   「v2 はパス前方一致で判定するため、将来 `/mfa` を接頭辞に持つ別ルートを足すと
   **MFA 未検証のまま到達できる**」として**前方一致を明示的に却下**し、ホワイトリスト方式を採った。
   §6.6 の表が v2 の表現をそのまま残しているため、**同じ文書内で 2 つの判定方式が併記されている**。
   → 「**§6.7 のホワイトリストに載るルートのみ許可**」に書き換える。
   併せて**社内管理者側の MFA 未検証 (4 系統目) の 401 も同じ行で扱えるようにする** (現在は
   ユーザー側の記述しかない)。
3. **重大 1 の修正で「MFA コード不正 = 401」「MFA 試行超過 = 429」の 2 行も同時に必要**になる。

### 中 6. `docs/design/auth.md:530`〜`:536` (§6.2 の MFA 新規実装表) — **v2 の MFA 実装の既知欠陥 2 件が「引き継がない」と書かれていない**。移植元として名指しした実装にそれが埋まっている

**該当**: `:536` (「登録・検証フロー | **ユーザー側の TOTP 実装が移植元になる**
(`POST /mfa/totp/generate` / `verify` / `reset`) | `hassan-v2-backend/router/router.go:231-233`」)。

**移植元に埋まっている欠陥** (§1.3 の N1 / N2 / N3。**auth.md §5 にも記載が無い**):

| # | 欠陥 | 出典 | v3 で必要な判断 |
|---|---|---|---|
| N1 | **MFA コード不一致が 500** — `VerifyTotp` は `apperror.TotpCodeNotMatch()` も含め全エラーを `internalServerError` へ流す | `hassan-v2-backend/controller/mfa.go:80`〜`:83` (`ResetTotp` も `:113`〜`:116`) | **401 にする** (中 5 の 3)。**500 のままだと総当たりが 5xx の山として現れ O-4 / O-7 が機能しない** |
| N2 | **MFA 検証失敗が失敗回数に加算されない** | `usecase/account/sign_in.go:91` が唯一の加算経路 | **重大 1** |
| N3 | **`POST /mfa/totp/reset` は現在の TOTP コードを要求する** — デバイス紛失時には使えない | `usecase/mfa/reset_totp.go:35`〜`:38` | `:536` が移植元に `reset` を含めているが、**紛失時の経路は `POST /accounts/mfa/reset` (管理者実行) の方**である (`:511` は正しくこちらを指している)。**同じ「MFA リセット」という語で 2 つの別機能を指している**ため、`:536` から `reset` を外すか「自己リセット (コード保持時)」と注記する |

**なぜ問題か**: §6.2 は v2 の MFA を移植元として名指ししつつ、**「v2 の機構を流用できない = 新規実装になる」
理由を器・クレーム・ミドルウェアの 3 点に限定している**。実装者は「フロー自体は v2 を写せばよい」と読み、
N1 / N2 をそのまま持ち込む。**§5 (v2 の欠陥一覧) に N1 / N2 が無いこと**が根本原因である
(§5 は 11 件あるが MFA 経路の欠陥は 1 件も入っていない — §0 の「未調査」に
「MFA の TOTP 検証ロジック内部」とあるが、**N1 / N2 はロジック内部ではなく呼び出し規約の問題**であり
調査範囲内だった)。→ **§5 に 5-12 (MFA 検証失敗が 500)・5-13 (MFA 失敗がロックカウンタに乗らない) を追加し、
§6.2 / §6.6 / §6.11-3 から参照する**。

### 中 7. `docs/design/auth.md:1242`〜`:1266` (§10) — **他書から auth.md への要求を受ける欄が無い**。DR-8 が「受信側の漏れ」として 5 巡目に再発した構造的原因

**事実**: §10 は「**本書の決定から派生**した他書への残作業」だけを持つ (R-1〜R-7 はすべて auth.md → 他書)。
一方 2026-07-31 時点で **auth.md 宛の是正要求が 5 件起票されている**:

| 起票元 | ID | 内容 | 本レビューでの扱い |
|---|---|---|---|
| `data-model.md:1136` | **R-DM-4** ②③④ | §6.3-1 の表と機械検査 / append-only の FK 例外 / `signup_links` の扱い | **②③は反映済み** (`:581`〜`:598` に DM-2 の強化と FK 例外がある。**R-DM-4 の状態列が stale**) / **④が重大 4** |
| `API/auth-accounts.md:538` | R-AA-1 | MFA 検証のレート制限 | **重大 1** |
| `同:539` | R-AA-2 ① | 公開エンドポイントの資格情報エラー = 401 + 本文 | 軽微 3 |
| `同:546` | R-AA-8 | `account_mfa_configs` を許可リストで扱う記述の見直し | 軽微 4 |
| `同:547` | R-AA-9 | 系統表にユーザー側の MFA 未検証が無い | **中 3** |
| `同:549` | R-AA-12 | §6.2 の列挙に一般アカウントの MFA リセットを追加 | **中 2** |

**なぜ問題か**: §10 に受信欄が無いため、**他書が起票した要求は auth.md 側からは追跡できない**。
4 巡目までの DR-8 は「自分が直した節の外の取り残し」で `grep` が有効だったが、
5 巡目の 11 件のうち 7 件は**外部からの未受信**であり、**auth.md 内の grep では検出できない**。
→ **§10.3「他書から本書への要求 (受信)」を新設し、起票元・ID・状態 (未対応 / 反映済み + 日付) を持たせる**。
`.claude/rules/06-delegation-prompts.md` の「是正要求の表は状態列を持たせる」を**受信側にも適用する**。
併せて **`data-model.md:1136` の R-DM-4 の状態列 (「②の §6.3-1 の表と③・④は未対応」) が
既に不正確 (②③は反映済み)** なので、起票元側も更新が必要。

---

## 4. 軽微 (Nice to Have)

| # | 箇所 | 内容 |
|---|---|---|
| **L1** | `docs/design/auth.md:1257`〜`:1266` (§10.2 の表) | **R の採番が順序も一意性も崩れている** — 表の行順は R-1 / R-2 / R-3 / **R-7** / R-4 / **R-5'** / R-6 / **R-5**。`R-5'` と `R-5` は**別の項目**であり (前者 = observability の actor 型、後者 = architecture の CORS)、「R-5 とは何か」を引くと節によって答えが変わる。→ **R-8 へ改番するか、行を ID 順に並べ替える** |
| **L2** | `docs/design/auth.md:794` (§6.7 の公開系統) | v2 の公開経路のうち **`POST /webhook/microcms/news`** (`hassan-v2-backend/router/router.go:192`。HMAC 署名検証) が列挙に無い (4 巡目 **L3 として指摘済み・未対応**)。`docs/design/API/news.md` は v2 backend が受信すると書いているため v3 に来ない判断はありうるが、**ホワイトリストは有限列挙なので「来ない」ことを 1 行書く**方が検査の実装者に伝わる |
| **L3** | `docs/design/auth.md:765`〜`:767` (§6.6 の 401 行) | 「本文 = **なし**」だが `docs/design/API/auth-accounts.md:306` (AA-D-9) が**資格情報エラーは 401 + `CodedError` 本文**を採用し、`:539` (R-AA-2 ①) で本節への例外明記を要求している (**未対応**)。FE がサインイン失敗の文言を出せるかに関わる。→ §6.6 に「**公開エンドポイントの資格情報エラーのみ本文を返す**」の例外行を足す (**別レビュアーの担当範囲と重複する可能性あり**) |
| **L4** | `docs/design/auth.md:627` (§6.3 の `account_mfa_configs` 行) | 「引くときの条件がトークン検証済みの `account_id` であるため **§6.4 の許可リスト側で扱う**」は不正確 — §6.4 の SQL 検査は**所有者条件 (`account_id` を含む) の有無**を見るため、`WHERE account_id = $1` を持つクエリは**検査を通り許可リストへの登録は不要**である。必要なのはスキーマ検査 (`contract_id` 必須) の除外だけ。→ 「本検査 (スキーマ側) の例外であり、**§6.4 の許可リストには載せない**」に訂正 (`API/auth-accounts.md:546` = R-AA-8 と同旨) |
| **L5** | `docs/design/auth.md:558`〜`:561` と `:936`〜`:939` | **同じ却下 2 案がラベルだけ変えて二重に書かれている** ((a)(b) = 運用手順書の DB 直更新 / 時間経過による自動解除、(c)(d) = 同じ 2 案)。4 巡目 **L5 として指摘済み・未対応**。ルート `CLAUDE.md`「同じ事実を 2 箇所に書かない」に照らし **§6.9 の回復経路節に集約**して §6.2 は参照にする |

---

## 5. `review-auth-api.md` の未解決 2 件の決着材料

### 5.1 重大 3 (§6.4 / §6.6 とリソース単位ロールの両立) — **両立する。書き方を変えれば読める**

**結論**: 両立する。**§6.4 が却下したのは「read-then-compare を*主たる防御*にすること」であり、
「取得後にロールを評価すること」自体ではない**。その証拠が §6.4 自身の**種別⑥**である
(`:713`。「一意キーで引いた直後に契約検証を行うクエリ」を**登録条件付きで許可**している)。
したがって必要なのは新しい方針ではなく、**「2 段の防御」として明示的に構造化すること**である。

| 段 | 何を守るか | 実装 | 機械強制 |
|---|---|---|---|
| 1 段目 (**テナント境界**) | 他契約のボードが 1 行も出ないこと | Repository のクエリ条件 `WHERE contract_id = $1` (§6.4 のとおり) | §6.4 の SQL 検査 (所有者条件の有無) |
| 2 段目 (**リソース単位ロール**) | 契約内でメンバーでない者に 404 / viewer に 403 | **entity の単一メソッド** (`HasAccess` / `CanEdit` / `IsAdmin` 相当) を UseCase が必ず呼ぶ | **操作 × ロールの全組み合わせを表駆動 UT で網羅** (`API/idea-boards.md` §3.1 の権限表をテストの入力にする) |

**採用案**: **§6.4 の層表に「リソース単位ロールを持つドメイン」の行を追加し、§6.6 の断定を限定する**。

1. **§6.4 に追加する行** (v2 の実装が既にこの形であることを出典付きで書く):
   「**リソース単位ロールを持つドメイン (アイデアボード)**: (a) Repository は `contract_id` で絞る —
   **メンバーシップを `WHERE` に入れない** (入れると viewer も 0 件になり 403 を返せない)
   (b) ロール判定は **`entity/` の単一メソッド**に集約し、UseCase から必ずそれを呼ぶ
   (v2 も同形: `hassan-v2-backend/usecase/idea_board/list_idea_boards.go:28` で
   `ListIdeaBoardsByContractID` (`WHERE contract_id = $1`) → `:44` の `if b.HasAccess(input.AccountID)`。
   判定メソッドは `hassan-v2-backend/entity/idea_board.go:113`〜`:126` の 3 本)
   (c) **操作 × ロールの全組み合わせを表駆動 UT で網羅し、D-2 のマージ条件に含める** —
   ロール判定の漏れは SQL 検査でも型でも検出できないため、**これが唯一の機械強制**である」。
2. **§6.6 の `:779` を限定する**: 「403 と 404 の取り違えが構造的に起きない」→
   「**個人スコープ / 契約スコープでは構造的に起きない。リソース単位ロールを持つドメインでは
   `entity/` の単一メソッド + 網羅 UT で担保する** (§6.4 の第 3 パターン)」。
3. **§6.4 の機械強制の箇条書きに 1 項追加**: 「リソース単位ロールの網羅 UT を D-2 のマージ条件に含める」。

**却下案**: **(a) リソース単位ロールをやめて契約スコープ + 個人スコープの 2 段に統一する** —
v2 の `idea_boards` に `viewer_account_ids` / `editor_account_ids` の**本番データが既にあり**
(`hassan-v2-backend/db/schema.sql:604`〜`:605`。照合済み)、統一すると viewer が editor に昇格するか
全員がアクセスを失う。`API/idea-boards.md` D-IB-8 が「**サイレントな権限昇格**」として却下済み。
**(b) メンバーシップを `WHERE` に入れて全部 404 にする** — 403 が消えるので取り違えは起きないが、
「共有されたのに見えない」と区別できず**共有機能の UX が壊れる**。また `API/README.md:346` の
「403 は 11 本」が誤りになる。

**この決着で閉じるもの**: `review-auth-api.md` 重大 3 / 同 中 10 (テーマメンバーの権限が未定義) の一部
(第 3 パターンの (c) がテーマメンバーにも適用される)。

### 5.2 重大 7 (共有・公開 A-7) — **第 1 リリースに含めるべき。先送りは不可能**

**結論**: **先送りできない**。理由は 3 つで、いずれも実測に基づく。

| # | 理由 | 出典 |
|---|---|---|
| 1 | **v2 の共有はボード限定ではなく、契約 × 3 カテゴリ (`idea` / `asset` / `business_plan`) の読み取りスコープを決めるゲートで、18 の読み取り経路が参照している** | §1.3 の N4 (`usecase/theme/list_themes.go:44` / `usecase/asset/{list_assets.go:72,get_asset.go:48,list_recent_used_assets.go:70}` / `usecase/idea/get_idea.go:60` / `usecase/idea_board/{list_idea_boards.go:32,list_ideas_for_board.go:66}` / `usecase/business_plan/` 6 件 / `entity/sharing_setting.go:38`〜`:47`) |
| 2 | **既定が非共有であるため、既定値を決めないと切替が「一斉公開」か「一斉非公開」のどちらかになる** | `hassan-v2-backend/usecase/theme/list_themes.go:42`〜`:44` のコメント (「レコード未作成時は false 扱い」) / `API/README.md:79` (F-16) |
| 3 | **`idea_boards` の 3 段ロールは本番データを持つ配列カラムであり、移行時に写像規則が必要** | `hassan-v2-backend/db/schema.sql:599`〜`:613` (`viewer_account_ids` / `editor_account_ids`。照合済み) |

**そして設計はもう存在する** — 先送りの是非を議論する段階ではなく、**SSOT がそれを宣言していないだけ**である:

| 決定 | 既存 SSOT |
|---|---|
| `sharing_settings` を v3 に引き継がず、**per-resource `visibility` (`private` / `contract`。既定 `private`) に置き換える** | `data-model.md:105`,`:125` (DM-9) |
| **列は増分 1 でスキーマに用意し、書き込み API は増分 2** | 同 DM-9 / `API/README.md:116` (D-API-8') |
| **既存 `sharing_settings` の値を移行時の `visibility` 初期値にする** (`is_shared=true` → `contract` / それ以外 → `private`) | `API/themes.md:136` (TM-1) / `API/assets.md:146` (AS-M1) / `API/settings.md:219` (ST-Q5) |
| **移行の実行タイミング** | `operations.md:458`,`:822` |
| **アイデアボードは v2 の契約内共有と 3 段ロールを増分 1 でそのまま引き継ぐ** | `API/idea-boards.md:182` (D-IB-11) / `:192` (§3.1) / 同 §4 M-4 |
| **契約外公開 (`open`) は採らない** | `API/themes.md:94` (D-TH-3) |

**採用案**: **auth.md §6 に §6.12 を新設し、上記 6 点を「単一の判断」として宣言する**
(内容は各 SSOT への参照で足り、再定義しない)。§7 の A-7 行を「**回答 (§6.12)**」に更新する。
**対応 AC が無い**ため (`:1086` の「— (対応 AC なし)」)、
`requirements.md` §3.1 に **AC を 1 本追加する** — 「**切替の前後で、契約内から見えるリソースの集合が
変わらないこと** (v2 の `sharing_settings` の値から `visibility` 初期値が決まり、
`is_shared=false` の契約で他人のリソースが 1 件も見えないことを移行検証で確認する)」。
これが無いと A-7 は `make check-traceability` の対象外のまま残る (DR-6 の逆方向)。

**却下案**: **(a) 「本増分では共有機能を持たない」を維持する** — 理由 1〜3 のとおり
**機能退行かテナント内一斉公開のどちらかが必ず起きる**ため成立しない。
**(b) A-7 の判断を `API/idea-boards.md` に委譲する** — ボードの 3 段ロールは書けるが、
**テーマ・アセット・企画書の 18 経路をカバーしない** (同書のスコープ外)。
また `auth.md:13` の SSOT 宣言と矛盾したままになる。
**(c) 判断を API-Q3 のユーザー確認待ちに置き続ける** — **既にすべての下流文書が
具体設計を確定させており、確認すべき未知が残っていない** (残る未確認は
`API/README.md:484` が併記する **テーマメンバーの権限差 (TH-Q5)** のみ)。

**先送りにする場合のスキーマ後方互換リスク** (指示の要求により明記する。ただし上記のとおり
先送りは推奨しない): `visibility` 列を増分 1 のスキーマに入れずに後から追加すると、
①**増分 1 で作られたリソースの既定値を後付けで決めることになり**、その時点で「非公開扱いにする =
v2 で共有していた組織が退行」「公開扱いにする = 非共有だった組織が露出」の二択が**移行ではなく
運用中の変更として発生する** (`data-model.md:125` の却下 (b) が指摘した「二重管理」)
②`sharing_settings` の既存値は **v2 の DB にしかない**ため、v2 退役後に初期値の決定材料が失われる
③`idea_boards` の配列カラムからの写像 (M-4) は**移送 1 回きり**の処理であり、
後続増分に回すと**移送済みデータに対する後付けマイグレーション**になる (`data-model.md` §6.4 の
「写像できなかった件数 0」を再度満たす必要が生じる)。

---

## 6. §10.2 の R-1〜R-5 (+ R-6 / R-7) の現在の状態 — 実測判定

**判定方法**: 各 R が委譲した先の文書を開き、該当節の実体と `[Answer]` の有無を確認した。

| # | auth.md `:1259`〜`:1266` の状態 | **実測した状態 (2026-07-31)** | 根拠 (パス:行) | 判定 |
|---|---|---|---|---|
| **R-1** (アカウント基盤の二重化) | 「未着手」。決めること ①〜⑤ | **回答済み — ①〜⑤すべて確定** (①v3 を正とする ②RL-3 の最初 ③v2 のアカウント更新系を数分停止 ④**同期しない (1 回コピーのみ)** ⑤切り戻しは v2 をそのまま使う。告知付き) | `docs/design/data-model.md:988`〜`:1005` (§6.5 の表 + `[Answer]`「**推奨 5 点すべてで確定 (2026-07-31 ユーザー回答)**」) | **stale。§9.1 の依存 3 項目・§6.3 の「最終形」・Q-A3 の残存リスクが連動して直せる** → 中 4 / 重大 4 |
| **R-1 の付帯** (§6.3 の例外列挙の確定) | 「R-1 で確定する」 | **確定可能。かつ `signup_links` の扱いが逆転済み** (DM-A4=B) | `docs/design/data-model.md:1021`〜`:1024` / `:389` / `:410` | **未反映** → **重大 4** |
| **R-2** (FE 設計: 両系統トークン) | 「未着手」 | **部分的に受領** — `API/settings.md:132`,`:144` が「併用期間中は FE が両系統のトークンを保持する」を記載し auth.md §10.2 R-2 を参照。**ただし `frontend.md` 側に方式 (どこに保持し・どう切り替えるか) の記述は未確認** | `docs/design/API/settings.md:132`,`:144` | **一部受領。方式は未着手** (本レビューでは frontend.md の該当節を精読していない = **未調査**) |
| **R-3** (認証系 API の入出力仕様 = Task-3i) | 「未着手」。**前提: R-1 の決定** | **完了に近い** — `docs/design/API/auth-accounts.md` が **37 エンドポイント**を確定 (§2 の系統別 4 節 + §3 の決定 AA-D-1〜15 + §3.5 の許可リスト 13 行 + §5 の是正要求 13 件)。R-3 の「必ず含めること ①〜④」のうち **①メンバー一覧のロック状態 / ②許可リストの確定 / ③社内管理者の MFA フロー / ④SuperAdmin 限定 (403)** はいずれも同書に記述がある | `docs/design/API/auth-accounts.md:163`,`:183`,`:271`〜`:274`,`:425`〜`:462`,`:530`〜`:551` / `data-model.md:1027` (「Task-3i の起草ブロックが解けた」) | **stale (「未着手」は誤り)。ただし同書は 1 巡目レビュー中であり「完了」とは書けない** |
| **R-7** (operations.md: 社内管理者の運用手順) | 「未着手」 | **未着手を確認** — `grep -rn "admin_accounts\|社内管理者" docs/design/operations.md` の結果に該当記述なし (4 巡目 M6 の指摘時点と同じ)。**加えて R-7 の項目に「初期パスワードの生成・配布・有効期限」が入っていない** | `docs/design/auth.md:1262` / `docs/design/operations.md` | **未着手 (正しい)。ただし項目が不足** → **重大 6** |
| **R-4** (設定値の SSOT) | 「未着手」 | **未着手を確認** (実装リポ宛のため本リポジトリに成果物は無い)。**制約に第 3 のしきい値 (MFA 試行上限) を追加する必要がある** | `docs/design/auth.md:1263` | **未着手 (正しい)。項目が不足** → **重大 1 の 4** |
| **R-5'** (observability: 監査 actor の型) | 「未着手」 | **反映済み** — 4 巡目 §14 の表が `observability.md:225`〜`:229` に `actor_type` が入ったことを報告し、同レビューが「解消」と判定している | `aidlc-docs/reviews/productionization/review-auth-hardening.md:1786` (BE-10 表の「監査の actor 型 → 解消」) | **stale (反映済み)** |
| **R-6** (infrastructure: レート制限カウンタの受け皿) | 「未着手」 | **未着手を確認** (`infrastructure.md` に未回答 `[Answer]` が 2 件 = `:519` / `:535`。該当かは未検証) | `make doc-lint` の出力 | **未着手 (正しい)。ただし本レビューでは infrastructure.md の該当節を精読していない = 未調査** |
| **R-5** (architecture: CORS の論点が消える) | 「未着手」 | **反映済み** — `API/README.md:483` の API-Q2 が「**不要になった (2026-07-30)**: D-ST-1' により … FE が v2 を叩く前提が消えた」と取り消し線付きで更新済み | `docs/design/API/README.md:483` | **stale (反映済み)** |

**集計**: **9 項目中、状態列が正しいのは 3 件 (R-7 / R-4 / R-6)**。
**stale が 4 件 (R-1 / R-1 付帯 / R-3 / R-5' / R-5 = 5 件)**、**一部受領が 1 件 (R-2)**。
**R-1 / R-1 付帯の stale は設計判断の宙吊りを生んでいる** (重大 4 / 中 4)。
それ以外は状態記述のみの陳腐化であり、**指示にある「既知の未反映事項 (未着手 12 箇所)」の範囲**として
新規の重大には数えない。

---

## 7. 本番観点カバレッジ (`.claude/rules/08-production-gates.md` の **A 領域全 ID**)

| ID | 状態 | 箇所 | 前巡 (4 巡目 §14.6) からの変化 |
|---|---|---|---|
| **A-1** 認証方式 | **回答** | §6.1 (v2 踏襲 + `golang-jwt/jwt/v5` + 空 role 拒否) / §6.8 (鍵の新規発行・複数鍵ローテーション) / §6.9 (7 日 + 手動ロック) / §6.10 (`crypto/rand`) / §6.11 (応答マスク・レート制限) | **後退 1 点** — §6.2 で MFA を新設したが、**MFA 検証の試行上限が無い** (重大 1)。A-1 の「方式の踏襲に伴う条件」(§7 の A-1 行) に MFA 必須化を数えているため、条件が 1 つ欠けた状態 |
| **A-2** ロールと適用範囲 | **部分回答** | §6.2 (一般ユーザー + 社内管理者の例外 5 点) / §6.7 (4 系統ホワイトリスト) / §9.3 Q-A2 (v2 で管理者限定だった 9 操作) | **範囲の宣言が 3 箇所で不一致のまま** (4 巡目 B1 後半と同じ指摘): §6.2 の 5 点表に**一般アカウントの MFA リセットが無い** (中 2) / §6.7 の系統表が**主体を書いていない** (中 3) / **ユーザー側の MFA 未検証系統が表に無い** (中 3) |
| **A-3** テナント境界 | **部分回答** | §6.3 (`contract_id` 必須 + 個人境界は `account_id` 追加 / 例外の有限列挙 / append-only の FK 例外) | **前進** (DM-2 の強化と FK 例外が反映された = R-DM-4 の②③)。**ただし除外リストの件数が 3 文書で 8/9 に分裂し `signup_links` の扱いが逆** (重大 4)。**CI 期待値のずれとして実装リポに出る** |
| **A-4** 絞り込みの層 | **部分回答** | §6.4 (Repository のクエリ条件 / 許可リスト 7 種 / 所有者 ID の 2 経路 / 専用型) | **F-15 型は閉じた** (4 巡目 B3 の 2 経路コンストラクタが `:673`〜`:690` に入り、`review-auth-api.md` 重大 6 の原案どおり)。**残る 2 点**: **書き込み系が検査外** (重大 5) / **リソース単位ロールとの両立が未記述** (重大 3) / `architecture.md:788` が 2 種のまま (中 1) |
| **A-5** ステータスコード | **部分回答** | §6.6 (401/403/404 + 429) | **503 が表に無い** (4 巡目 M3 未対応) / **MFA 検証失敗の行が無い** (中 5・中 6) / **`/mfa` 配下という前方一致の表現が §6.7 の却下と矛盾** (中 5) |
| **A-6** LLM への越境 | **参照 (SSOT は `architecture.md` §3.8.2)** | §6.5 | 変化なし。**ただし §6.5 は「検証の実体は §6.4 の Repository 規約と同一」と書いており、重大 5 (書き込み系が検査外) の影響を受ける** — ツールが書き込みを行う設計になった場合に穴が伝播する |
| **A-7** 共有・公開 | **未回答 (SSOT が空白)** | §7 の A-7 行のみ (「対象外 (暫定)」) | **後退** — `architecture.md:791` が旧判断を**取り下げ**、auth.md はその取り下げられた判断を根拠にしている。**判断を持つ文書が 1 つも無い** (重大 2)。対応 AC も無い |

**参照している他領域の ID** (本書が回答すると宣言している範囲のみ):

| ID | 状態 | 備考 |
|---|---|---|
| **D-5** (認証固有部分) | **回答** | §6.8 (v3 で鍵を新規発行 / Secrets Manager / 複数鍵検証 / ユーザー用と管理者用を分離)。**変化なし・後退なし** |
| **D-2** (CI ゲート。SSOT は `architecture.md` §5) | **部分** | `math/rand` 検査は登録済み (`:1252`)。**許可リスト 7 種と検査 4 種の拡張が `architecture.md:788` に届いていない** (中 1)。**書き込み系検査とリソース単位ロールの網羅 UT は未登録** (重大 5 / 重大 3) |
| **O-4 / O-6 / O-7** | **参照 (委譲)** | §6.9 / §6.11-4 → `observability.md`。**MFA 検証失敗が 500 で返る前提だと O-4 / O-7 が機能しない**点が未記述 (中 6) |
| **D-8** (WAF) | **参照 (委譲)** | §6.2 の③ (IP 許可リスト) / §6.11-3。**FE-Q7 との矛盾は 3 巡目で既指摘・ユーザー判断待ちのため再指摘しない** |

**無言の省略 (DR-2) の判定**: **1 件 = A-7**。「対象外」と書かれてはいるが、
**その根拠が取り下げられた文の参照**であり、かつ**下流 6 文書が具体設計を持っている**ため、
実質は「判断の不在」である。A-1〜A-6 / D-5 には回答か理由付きの委譲がある。

---

## 8. DR / BE / FE パターンの判定 (`.claude/rules/feedback_review_patterns.md`)

| # | 判定 |
|---|---|
| **DR-1** 出典なしの断定 | **良好**。抜き取り 14 件すべて一致 (§1.2)、4 巡目 L1 の行番号誤りも訂正済み。**ただし「書かれていない事実」が 3 件ある** — MFA 経路の欠陥 N1 / N2 / N3 が §5 の 11 件に入っていない (中 6)。出典の正確さは高いが**カバレッジに穴**がある |
| **DR-2** 本番観点の無言の省略 | **1 件 = A-7** (重大 2)。他の A / D / O は回答か ID 付きの委譲がある |
| **DR-3** 既存データの不在 | **1 件 = A-7 の既存データ** (重大 2 の理由 1〜3。`sharing_settings` の 18 経路 + `idea_boards` の配列カラム)。**R-1 (アカウントの二重化) は data-model.md §6.5 でロールバックまで含めて解決済み** |
| **DR-4** PoC 実装のコピー設計 | **無し**。gin / sqlc / `CodedError` / 4 層 + entity/gateway と整合 |
| **DR-5** 曖昧語による丸投げ | **2 件**: ①「**MFA を必須にする**」は上限の定義が無いため判断ポイントとしては未完成 (重大 1。4 巡目 B1 で同じ判定が出て「新規実装になる」ことは書かれたが**上限は依然として未定義**) ②`:512` の「初回サインイン後に TOTP 登録を強制する」は**強制の期限・初期パスワードの出所が無い** (重大 6)。`grep -nE "適切に\|必要に応じて\|後で検討\|別途検討"` → **0 件** |
| **DR-6** AC の宙吊り / 逆方向 | **逆方向 2 件**: **A-7 に対応する AC が無い** (`:1086` の「— (対応 AC なし)」。重大 2 の修正案 4) / **MFA 試行上限に対応する AC が無い** (AC-1.6 はレート制限の実装層と超過時の応答のみ)。`make check-traceability` は **47/47 カバー・未カバー 0** |
| **DR-7** プロトタイプを仕様として扱う | **無し** (auth.md はプロトタイプを参照していない) |
| **DR-8** 修正の波及漏れ | **11 件 (§1.4)**。**5 巡連続で最上位**。性質が変化し、**7 件が「外部文書からの未受信」** — §10 に受信欄が無いことが構造的原因 (中 7) |
| **DR-9** 件数・集合サイズの転記 | **1 件 = 除外リストの件数** (重大 4)。**`make check-table-counts` の検算対象に入っていない新しい「N 件」**であり、DR-9 の運用注記「新しく『N 件』を書くときは、同時に検算の対象に加えるか、書かずに定義元へのリンクにする」に対する**未達**。40/32/8 の 3 値は検算済みで整合 |
| **BE-2** 設定値の SSOT | **対応済み + 1 点不足**。R-4 にしきい値の SSOT と「ロックしきい値 < レート制限しきい値」の制約がある。**MFA 試行上限という第 3 のしきい値が入るため 3 者の関係を書く必要がある** (重大 1 の 4) |
| **BE-10** 読む側と書く側を対で設計する | **6 度目**。今回は ①**MFA を防御に据えたが試行上限を書く側が無い** (重大 1) ②**MFA 登録を強制すると決めたが初期パスワードを配る側が無い** (重大 6) ③**「すべてのクエリが所有者条件を持つ」と書いたが書き込み系を検査する側が無い** (重大 5)。**4 巡目 §14.4 が提案した「決定 → 実装先 → 委譲先での受領確認」の 3 列表は §6.2 / §6.11-1 に導入されていない** |
| **BE-8** schema と handler の乖離 | 直接の該当なし (LLM ツール側の論点。A-6 は `architecture.md` §3.8.2 が SSOT) |
| **FE-x** | 本書の範囲外 (§6.9 の FE トークン保管は調査結果として正確。`hassan-v2-frontend` の照合は 4 巡目までに完了) |

---

## 9. Design Freeze の可否

**判定: `docs/design/auth.md` スコープの Design Freeze は不可**。

### 9.1 Freeze の必要条件 (これを満たせば Freeze 可)

| # | 条件 | 対応する指摘 | 規模 |
|---|---|---|---|
| **F1** | **MFA 検証の試行上限を §6.11-3 の対象に含め、§6.2 に「MFA を主たる防御にすることの成立条件」として明記する**。併せて §6.6 に「MFA コード不正 = 401」「MFA 試行超過 = 429」の 2 行を足す | 重大 1 / 中 5 / 中 6 | §6.11-3 の 1 行改訂 + §6.2 に 1 行 + §6.6 に 2 行 |
| **F2** | **§6.12 (A-7) を新設して単一の判断を書き、§7 の A-7 行を「回答」に更新する**。`requirements.md` に AC を 1 本追加 | 重大 2 | 新節 15〜20 行 (内容は既存 SSOT への参照) |
| **F3** | **§6.4 に「リソース単位ロールを持つドメイン」の第 3 パターンを追加し、§6.6 の `:779` の断定を限定する**。網羅 UT を D-2 のマージ条件へ | 重大 3 | §6.4 に 1 行 + §6.6 の 1 文改訂 + 箇条書き 1 項 |
| **F4** | **除外リストの件数の転記を全廃し (auth.md `:601`,`:603` / `architecture.md:764`)、`signup_links` を §6.3 の例外表から §6.4 の許可リスト①へ移す**。併せて `:633` / `:1082` の「R-1 で確定する」を削除して列挙を確定させる | 重大 4 / 中 4 | 3 文書で計 5 行の改訂 |
| **F5** | **§6.4 の CI 検査対象に書き込み系 (`Update*` / `Delete*`) を加え、`:653` の「すべてのクエリ」と範囲を一致させる**。`API/README.md:158` も同時に更新。`INSERT` の扱いを 1 行で決める | 重大 5 | 2 文書で計 3 行 |
| **F6** | **§6.2 に社内管理者の初期パスワードの生成・配布・有効期限と、登録窓の閉じ方を書く** (v2 のパスワードを移送しない / `crypto/rand` の一時パスワード / N 時間で失効 / 初回登録を監査記録に残す)。R-7 の項目にも追加。**N5 (v2 のロック解除に SuperAdmin 判定が無い) を事実として書き、v3 の判断を明示する** | 重大 6 | §6.2 に 1 表 (5〜6 行) + R-7 に 1 項 |

**F1〜F6 はいずれも節の書き足し / 表への行追加 / 文言の限定であり、設計方針のやり直しではない**
(合計 40〜50 行規模)。**方針レベルで再検討が必要なものは無い** — 認証方式・鍵・失効・乱数・
レート制限の実装層・所有者列の規約は 5 巡を通じて安定しており、後退していない。

### 9.2 Freeze 後 / 実装リポで扱ってよいもの

中 1〜中 7 のうち **中 1 / 中 2 / 中 3 / 中 5 / 中 6** は F1〜F6 の作業と同じ差分で閉じられる。
**中 4 (R-1 の stale)** と **中 7 (§10.3 受信欄の新設)** は Freeze の可否には影響しないが、
**中 7 を入れないと 6 巡目の DR-8 が確実に再発する** (今回の 11 件のうち 7 件が受信漏れだったため)。
軽微 L1〜L5 は実装リポへの引き渡し前にまとめて処理すればよい。

### 9.3 再レビューの範囲

- **F1 / F6 (MFA 周辺)** は**再レビュー必須** — 上限・初期パスワード・窓の設計はいずれも
  「決めた防御に実行主体が付いているか」の判定を要し、`make check` では検証できない。
- **F2 (§6.12)** は**再レビュー必須** — 6 つの既存 SSOT を単一の判断に集約する作業であり、
  集約漏れ (特に `business_plan` カテゴリ = v3 の企画書ドメイン) が起きやすい。
- **F3 / F4 / F5** は表への行追加と件数の削除であり、**`make check` の再実行 + grep の証跡**で足りる
  (F4 は「9 件」「7 件」「`signup_links`」の 3 語で `docs/` を grep し 0 件になることを証拠にする)。

---

## 10. 良かった点

1. **4 巡目のブロッカー 3 件 (B1 / B2 / B3) はいずれも実体として閉じている**。自己申告と実物の乖離は無かった —
   B1 の MFA 表 4 行 (`:531`〜`:536`)・B2 の許可リスト種別⑥⑦ (`:713`〜`:714`)・
   B3 の 2 経路コンストラクタ (`:673`〜`:690`) はすべて実在し、**引用された出典 14 件すべてが一次ソースと一致した**。
   `review-auth-api.md` の **重大 6 (F-15 型の越境) は完全に閉じた** — 「存在確認は所有権の検証にならない」の
   機構が `:683`〜`:690` に v2 の実例 (`usecase/theme/list_themes.go:56`〜`:62`) 付きで書かれている。
2. **許可リストの種別⑥が「read-then-compare を登録条件付きで許可する」という枠を既に作っていた**。
   これは重大 3 (リソース単位ロール) の決着を**新方針なしで**可能にしており、
   設計の抽象化が偶然ではなく効いている例である (§5.1)。
3. **§6.10-3 の検査範囲の決め方が模範的** — 「認証パッケージのみにしない」理由を
   「v2 の欠陥は汎用の `util/util.go` にあり、限定した検査ではこの実例を 1 件も検出できない」という
   **実例による反証**で示している (`:988`)。**同じ論法を書き込み系クエリに適用していないのが重大 5** であり、
   つまり正しい基準は文書内に既にある。
4. **§5-8 の深刻度の限定が誠実**。`math/rand` を「予測可能」と誤認せず、ローカルの Go 標準ライブラリを
   読んで「最悪ケースには当たらない」と確定させたうえで、**それでも使わない理由を 3 点** (GODEBUG 依存 /
   API 契約の範囲外 / 回避コストが実質ゼロ) に整理している。**照合したところ `util/util.go:3`〜`:5`,`:30`〜`:36` と
   `create_admin_account.go:64`,`:90`,`:95`〜`:100` の記述はすべて正確**だった。
5. **「無い」の断定範囲の書き分け** (§6.9 の「未発見であり存在しないことの証明ではない」、
   §6.11-3 の「アプリ層には無い。インフラ層は未確認」) が維持されている。
   5 巡を通じて**この規律が緩んでいない**。
6. **§6.11-3 のカウンタ保管先の決定**が、他書の確定事実 (`infrastructure.md` INF-E の `desiredCount = 2`) を
   引いて「プロセス内メモリにすると実効しきい値がタスク数倍になる」まで導出している。
   **決定の理由が検証可能な形になっている**好例。

---

## 総合判定

**重大 6 件 / 中 7 件 / 軽微 5 件 — `docs/design/auth.md` スコープの Design Freeze は不可**。

**性格**: 5 巡を経て**認証方式・鍵・失効・乱数・所有者列の規約は安定しており、後退は無い**。
今回の重大 6 件は 3 つの型に分類できる:

| 型 | 件数 | 内容 |
|---|---|---|
| **決定した防御に上限・実行主体・検査が付いていない** (BE-10 の 6 度目) | 3 | 重大 1 (MFA に試行上限が無い) / 重大 5 (書き込み系が検査外) / 重大 6 (初期パスワードの出所と窓が無い) |
| **外部文書が 2026-07-31 に確定させた事実の未受信** (DR-8 の受信側) | 2 | 重大 4 (DM-A4=B / DM-A3 の未反映と件数の分裂) / 中 4 |
| **2 巡以上未対応の既存指摘** | 2 | 重大 2 (`review-auth-api.md` 重大 7) / 重大 3 (同 重大 3) |

**最も本番リスクが高いのは重大 1** — 「社内管理者にロックを設けず MFA を主たる防御にする」という
4 巡目の決定が、**MFA 検証の試行上限を伴っていない**。v2 の一次ソースで
「MFA 失敗はロックカウンタを増やさない」(`usecase/account/sign_in.go:91` が唯一の加算経路) と
「コード不一致が 500 になる」(`controller/mfa.go:80`〜`:83`) を確認しており、
**移植すれば全契約横断のロック解除 API が 6 桁の総当たりで抜け、その試行は 5xx の山に隠れる**。
`docs/design/API/auth-accounts.md` の AA-D-10 / R-AA-1 が同じ穴を独立に発見しているため、
**2 経路から同一の指摘が出ている**。

**修正規模は 40〜50 行**であり、方針のやり直しは不要。
**再レビューが必須なのは F1 / F2 / F6 (MFA 周辺と §6.12)**、F3〜F5 は `make check` + grep の証跡で足りる。

**未調査の範囲 (正直に記す)**: `docs/design/frontend.md` の R-2 該当節 (両系統トークンの方式) /
`docs/design/infrastructure.md` の R-6 該当節と未回答 `[Answer]` 2 件 (`:519` / `:535`) の内容 /
`docs/design/observability.md` の §4.5 全文 (R-5' の反映は 4 巡目レビューの記録に依拠した) /
`docs/design/API/auth-accounts.md` の内容妥当性 (**別レビュアーの担当**。auth.md 側から見た矛盾のみ抽出) /
v2 の TOTP 検証ロジック内部 (時刻ずれ許容幅・リプレイ防止。auth.md §0 の「未調査」と同じ範囲で、
**重大 1 の探索空間の見積り (10^6) はこの許容幅に依存する** — 許容幅が広ければさらに容易になる)。

---

### 付記: `make check` の再実行 (本レビュー成果物の追加後)

```
[doc-lint] 対象 91 ファイル / エラー 1 件 / 警告 36 件
```

**エラー 1 件は本レビュー成果物のものではない** — `aidlc-docs/reviews/productionization/review-auth-accounts.md`
のリンク切れ (`API/auth-accounts.md`)。同ファイルは**別レビュアーが並行編集中**であり、
レビュー開始時点 (§1.1 の出力) では存在しなかった。本ファイル (`review-auth-round5.md`) の
doc-lint エラーは **0 件**である (執筆時に発生した相対リンク 3 件は、引用元の記法をコードスパンへ
置き換えて解消した)。`check-traceability` (47/47・24/24) / `workflow-shell` (52 ブロック) /
`table-counts` (照合 22 件) はいずれも変化なし。

---

## 指摘の反映記録 (2026-07-31 夜・メインセッション)

対象は `docs/design/auth.md` (+ 波及先の `architecture.md` / `data-model.md` / `API/settings.md` /
`aidlc-docs/inception/productionization/plan.md`)。**状態列を持たせる** (`.claude/rules/06-delegation-prompts.md`)。

| 指摘 | 反映先 | 状態 |
|---|---|---|
| **重大 1** MFA 試行回数に上限が無い | `auth.md` §6.2 (新設ブロック「この判断の成立条件」) / §6.11-3 (**対象①/②に分割**) / §6.6 (MFA 401・429 の 2 行) / §10.2 R-4 | **実施済み** — 「**MFA 検証の試行上限が、ロックを設けない判断の成立条件である**」と明記。しきい値の関係を **ロック < MFA 試行上限 < 未認証レート制限**と定め、**MFA はサインイン失敗と別カウンタ**にした (同一にすると打ち間違いがロックを引き起こす) |
| **重大 2** A-7 の判断を持つ文書が無い | `auth.md` **§6.12 を新設** + §7 の A-7 行を「回答」へ | **実施済み** — 下流 6 文書が既に持っていた設計 ((a) `sharing_settings` は引き継がず初期値に使う (b) ボードは 3 段ロールを増分 1 で引き継ぐ (c) `visibility` 書き込みは増分 2 (d) `open` は持たない) を SSOT として宣言。**「対象外」が成立しない定量的根拠 (18 の読み取り経路) を却下案として記録**。**実装範囲は増やしていない** |
| **重大 3** リソース単位ロールとの両立 | `auth.md` §6.4 (**「第 3 のパターン」を新設**) / §6.6 の帰結を限定 | **実施済み (決着案どおり)** — 却下 (a) の範囲を「**テナント境界の**主たる防御」に限定し、**境界は必ず SQL / 契約内ロールは `entity/` の単一メソッド + 表駆動 UT (5 ケース)** の 3 条件で許可。§6.6 の「構造的に起きない」も同じ範囲に限定した |
| **重大 4** 除外リストの 9/8 分裂・`signup_links` の反転 | `auth.md` §6.3 / `architecture.md` §4 / `data-model.md` (定義元) / `scripts/check-table-counts.sh` | **実施済み (推奨の「書かない」を採用)** — auth.md と architecture.md から**件数の転記を削除**し `data-model.md` §4.1.2 の 2 表を SSOT に。`signup_links` 行を「**検査を通る**」に反転し、未認証経路を §6.4 種別①へ。**併せて機械検算を追加** (照合 22 → 37 件) |
| **重大 5** CI 検査が読み取り 4 種のみ | `auth.md` §6.4 / Repository 行 / `architecture.md` §5 A-4 | **実施済み** — **`Update*` / `Delete*` を検査対象に追加**。§5-11 が UPDATE であることを根拠として明記。**`INSERT` は §6.3 の必須列で担保する**と明示 (無言の省略にしない) |
| **重大 6** MFA 登録前の窓がパスワードのみ | `auth.md` §6.2 (新設ブロック「初回登録の窓を閉じる」) / §10.2 R-7 | **実施済み** — ①**v2 の管理者パスワードを移送しない** (`crypto/rand` の一時パスワード + 帯域外配布) ②**窓に期限**を設け期限切れは無効化 ③未登録アカウントも §6.11-3 の対象② ④初回登録を監査記録に残す。**N5 の非対称も解消** — 全契約横断ロック解除を **SuperAdmin 限定**にした |
| **中 1** architecture.md の許可リストが 2 種のまま | `architecture.md` §5 A-4 行 | **実施済み** — 7 種を列挙 + 検査範囲 + 第 3 のパターンへの参照 |
| **中 2** 一般アカウントの MFA リセットが列挙に無い | `auth.md` §6.2 の含める範囲表 | **実施済み** (`POST /admin/accounts/{account_id}/mfa/reset` を 1 行追加。AA-Q2=a) |
| **中 3** 4 系統表が主体を書いていない | `auth.md` §6.7 | **実施済み** — 主体付きに書き換え + 「同名の操作を系統で読み分ける」注記。ユーザー側の MFA 未検証も記載 |
| **中 4** R-1 回答済みなのに宙吊り | `auth.md` §6.3 / §9.1 の依存表 / §9.3 Q-A3 / §10.2 R-1 の状態列 / `plan.md` | **実施済み** — 3 項目すべて「確定」へ。**Q-A3 は④「同期しない」で確定させたうえで、「コピーする中身は v2 由来」という残余リスクを新たに明記**し operations へ申し送った |
| **中 5** §6.6 に 503 が無い / `/mfa` 前方一致 | `auth.md` §6.6 | **実施済み** (503 の行を追加 / ホワイトリスト参照へ書き換え) |
| **中 6** v2 の MFA 欠陥 2 件が「引き継がない」と書かれていない | `auth.md` **§5 に 5-12 / 5-13 を新設** + §6.2 の移植元行 | **実施済み** — §5-12 (コード不一致が 500) / §5-13 (失敗が加算されない) を出典付きで追加。**`reset` を移植元から外し**「自己リセット」と「管理者リセット」を別機能として書き分けた |
| **中 7** 他書からの要求を受ける欄が無い | `auth.md` **§10.3 を新設** | **実施済み** — 起票元・ID・状態の受信表 (11 行)。**運用として「反映前に 1 行追加する」も明記**。`data-model.md` の R-DM-4 の状態列も更新 |
| **軽微 L1** R の採番が崩れている | `auth.md` §10.2 | **実施済み** — `R-5'` を **R-8** へ改番し ID 昇順に並べ替え。「詰め直し・再利用をしない」を明記 |
| **軽微 L2** webhook がホワイトリストに無い | `auth.md` §6.7 の公開系統 | **実施済み** — 「v3 に持ち込まない」を 1 行明記 |
| **軽微 L3** 401 の本文例外 | `auth.md` §6.6 | **実施済み** (公開エンドポイントの資格情報エラーのみ本文あり = 分類 C) |
| **軽微 L4** `account_mfa_configs` の扱いが不正確 | `auth.md` §6.3 | **実施済み** — 「**許可リストには載せない**」に訂正 (`WHERE account_id = $1` は検査を通る) |
| **軽微 L5** 同じ却下 2 案が二重 | `auth.md` §6.2 → §6.9 に集約 | **実施済み** |
| §5.2 **重大 7 の決着** (A-7 を第 1 リリースに含める) | — | **設計は §6.12 に反映済み**。ただし**「第 1 リリースのスコープに含める」という判断そのものはユーザー確認が必要** — §6.12 は「下流文書が既に持つ設計の宣言」であり実装範囲を増やしていないが、増分 2 との線引き ((c)) は据え置いた |
